<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Ceph 安装记录</title>
    <url>/2023/04/28/ceph/Ceph%20%E5%AE%89%E8%A3%85%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<p>ceph 安装记录</p>
<span id="more"></span>
<figure class="highlight bash"><figcaption><span>bash</span></figcaption><table><tr><td class="code"><pre><code class="hljs bash">sudo su<br>apt install -y python-is-python3 systemd chrony<br><br>chronyc sources<br><br>172.31.18.113 ceph-01<br>172.31.26.40 ceph-02<br>172.31.24.19 ceph-03<br><br><br><br><span class="hljs-comment"># 安装 docker</span><br>sudo apt-get install -y apt-transport-https  ca-certificates  curl  software-properties-common gnupg1 gnupg2<br>curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -<br>apt-key fingerprint 0EBFCD88<br>sudo add-apt-repository    <span class="hljs-string">&quot;deb [arch=amd64] https://download.docker.com/linux/ubuntu <span class="hljs-subst">$(lsb_release -cs)</span> stable&quot;</span><br>apt-get update<br>apt-get -y install docker-ce<br><br><br><span class="hljs-comment"># 下载</span><br>curl --silent --remote-name --location https://github.com/ceph/ceph/raw/quincy/src/cephadm/cephadm<br><span class="hljs-built_in">chmod</span> +x cephadm<br><br>./cephadm add-repo --release quincy<br><br>./cephadm install<br><span class="hljs-built_in">which</span> cephadm<br><br>cephadm shell<br>cephadm shell -- ceph -s<br><br>cephadm bootstrap --mon-ip *&lt;mon-ip&gt;*<br>cephadm add-repo --release quincy<br>cephadm install ceph-common<br>ceph -v<br>ceph status<br><br>ceph orch host label add *&lt;host&gt;* _admin<br><br>ssh-copy-id -f -i /etc/ceph/ceph.pub root@ceph-02<br>ssh-copy-id -f -i /etc/ceph/ceph.pub root@ceph-03<br><br>ceph orch host add ip-172-31-26-40 172.31.26.40 --labels _admin<br>ceph orch host add ip-172-31-24-19 172.31.24.19 --labels _admin<br><br>ceph orch apply osd --all-available-devices<br><br><br>ceph fs volume create fs-01 --placement=<span class="hljs-string">&quot;ip-172-31-18-113&quot;</span><br><span class="hljs-built_in">stat</span> /sbin/mount.ceph<br><br>mount -t ceph cephuser@.fs-01=/ -o secret=AQCR52Rjyuu7ChAAzZs+tBmFrp3bGvYAHZHwJg==<br><br><span class="hljs-comment"># 挂载cephfs 前提条件</span><br>ssh &#123;user&#125;@&#123;mon-host&#125; <span class="hljs-string">&quot;sudo ceph config generate-minimal-conf&quot;</span> | sudo <span class="hljs-built_in">tee</span> /etc/ceph/ceph.conf<br><span class="hljs-built_in">chmod</span> 644 /etc/ceph/ceph.conf<br>ssh &#123;user&#125;@&#123;mon-host&#125; <span class="hljs-string">&quot;sudo ceph fs authorize cephfs client.djx / rw&quot;</span> | sudo <span class="hljs-built_in">tee</span> /etc/ceph/ceph.client.foo.keyring<br><span class="hljs-built_in">chmod</span> 600 /etc/ceph/ceph.client.foo.keyring<br><br><span class="hljs-comment"># 挂载 cephfs</span><br>mount -t ceph djx@f97eeb7c-5bed-11ed-9dfb-05518952193e.fs-01=/ /mnt/fs-01 -o mon_addr=172.31.18.113:6789/172.31.26.40:6789/172.31.24.19:6789,secret=AQCR52Rjyuu7ChAAzZs+tBmFrp3bGvYAHZHwJg==<br><br><br><span class="hljs-comment"># 操作服务</span><br>ceph orch &lt;start|stop|restart|redeploy|reconfig&gt; &lt;service_name&gt;<br><br></code></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Ceph 学习</category>
      </categories>
      <tags>
        <tag>ceph</tag>
      </tags>
  </entry>
  <entry>
    <title>Ceph集群部署</title>
    <url>/2023/04/28/ceph/Ceph%20%E9%9B%86%E7%BE%A4%E9%83%A8%E7%BD%B2/</url>
    <content><![CDATA[<h1 id="一、Ceph-简介"><a href="#一、Ceph-简介" class="headerlink" title="一、Ceph 简介"></a>一、Ceph 简介</h1><blockquote>
<p>一个 Ceph 存储集群需要至少一个 Ceph 监视器、Ceph 管理器和 Ceph OSD(对象存储守护进程)。在运行 Ceph 文件系统客户端时，也需要 Ceph 元数据服务器。</p>
</blockquote>
<span id="more"></span>

<ul>
<li><p><strong>Monitors</strong>:</p>
<ul>
<li>Ceph 监视器(<code>ceph-mon</code>)维护着展示集群状态的各种图表，包括监视器图、管理器图、OSD 图、MDS 图和 CRUSH 图。这些图是 Ceph 守护进程相互协调所必需的关键集群状态。</li>
<li>监视器还负责管理守护进程和客户端之间的身份验证。为了实现冗余和高可用性，通常需要至少三个监视器。</li>
</ul>
</li>
<li><p><strong>Managers</strong>: <a href="https://docs.ceph.com/en/latest/glossary/#term-Ceph-Manager">Ceph Manager</a>守护程序(<code>ceph-mgr</code>)</p>
<ul>
<li>负责跟踪运行时指标和 Ceph 群集的当前状态，包括存储利用率、当前性能指标和系统负载。</li>
<li>Ceph Manager 守护程序还托管基于 python 的模块来管理和公开 Ceph 群集信息，包括基于 Web 的<a href="https://docs.ceph.com/en/latest/mgr/dashboard/#mgr-dashboard">Ceph 仪表板和</a> <a href="https://docs.ceph.com/en/latest/mgr/restful">REST API</a>。高可用性通常需要至少两个管理器。</li>
</ul>
</li>
<li><p><strong>OSDs</strong>: <a href="https://docs.ceph.com/en/latest/glossary/#term-Ceph-OSD">Ceph OSD</a> (object storage daemon, <code>ceph-osd</code>)存储数据，处理数据复制，恢复，重新平衡，并提供一些监视信息到 Ceph 监视器和管理器,通过检查其他 Ceph OSD 守护进程检测信号.冗余和高可用性通常需要至少 3 个 Ceph OSD。</p>
</li>
<li><p><strong>MDSs</strong>: <a href="https://docs.ceph.com/en/latest/glossary/#term-Ceph-Metadata-Server">Ceph 元数据服务器</a>（MDS, <code>ceph-mds</code>）代表<a href="https://docs.ceph.com/en/latest/glossary/#term-Ceph-File-System">Ceph 文件系统</a>存储元数据（即 Ceph 块设备和 Ceph 对象存储不使用 MDS）。Ceph 元数据服务器允许 POSIX 文件系统用户执行基本命令 (如<code>ls</code>, <code>find</code>, <code>etc</code>等)，而不会给 Ceph 存储群集带来巨大负担。</p>
</li>
</ul>
<blockquote>
<p>Ceph 将数据存储为逻辑存储池中的对象。使用<a href="https://docs.ceph.com/en/latest/glossary/#term-CRUSH">CRUSH 算法</a>，Ceph 计算哪个放置组应包含对象，并进一步计算哪个 Ceph OSD 守护程序应存储该放置组。CRUSH 算法使 Ceph 存储群集能够动态扩展、重新平衡和恢复。</p>
</blockquote>
<p>参考链接：</p>
<ul>
<li><a href="https://mp.weixin.qq.com/s/TWXPPk7hE1D4AGsHg7CIMg">https://mp.weixin.qq.com/s/TWXPPk7hE1D4AGsHg7CIMg</a></li>
<li><a href="https://www.jianshu.com/p/cc3ece850433">https://www.jianshu.com/p/cc3ece850433</a></li>
<li><a href="https://mp.weixin.qq.com/s/QXRdyYKSKIa1aLNI8dOGcQ">https://mp.weixin.qq.com/s/QXRdyYKSKIa1aLNI8dOGcQ</a></li>
<li><a href="https://blog.csdn.net/xiaoquqi/article/details/43055031">https://blog.csdn.net/xiaoquqi/article/details/43055031</a></li>
</ul>
<h1 id="二、Cephadm-部署集群"><a href="#二、Cephadm-部署集群" class="headerlink" title="二、Cephadm 部署集群"></a>二、Cephadm 部署集群</h1><p>简单，简述，详细参考：<a href="https://docs.ceph.com/en/latest/cephadm/#cephadm">https://docs.ceph.com/en/latest/cephadm/#cephadm</a></p>
<h2 id="1-环境规划"><a href="#1-环境规划" class="headerlink" title="1.环境规划"></a>1.环境规划</h2><table>
<thead>
<tr>
<th>主机名</th>
<th>内网 IP</th>
<th>操作系统</th>
<th>角色</th>
<th>配置</th>
</tr>
</thead>
<tbody><tr>
<td>ceph1</td>
<td>192.168.200.128</td>
<td>Debian11</td>
<td>cephadm,mon,mgr,osd</td>
<td>2C,2G,20G 系统盘，20G 数据盘</td>
</tr>
<tr>
<td>ceph2</td>
<td>192.168.200.131</td>
<td>Debian11</td>
<td>cephadm,mon</td>
<td>2C,2G,20G 系统盘，20G 数据盘</td>
</tr>
<tr>
<td>ceph3</td>
<td>192.168.200.132</td>
<td>Debian11</td>
<td>cephadm,mon</td>
<td>2C,2G,20G 系统盘，20G 数据盘</td>
</tr>
</tbody></table>
<blockquote>
<p>典型的 Ceph 集群有三个或五个监视器守护程序，分布在不同的主机上。如果群集中有五个或更多节点，我们建议部署五个监视器。</p>
</blockquote>
<p>当 Ceph 知道 mon 应该使用什么 IP 子网时，它可以随着群集的增长（或收缩）自动部署和缩放 mon。默认情况下，Ceph 假定其他 mon 使用与第一个 mon 的 IP 相同的子网。</p>
<p>在单个子网的情况下，如果向集群中添加主机，默认最多只会添加 5 个 mon 如果有特定的 IP 子网给 mon 使用，可以使用 CIDR 格式配置该子网。</p>
<h2 id="2-前置-所有节点"><a href="#2-前置-所有节点" class="headerlink" title="2.前置(所有节点)"></a>2.前置(所有节点)</h2><blockquote>
<p><a href="https://docs.ceph.com/en/pacific/cephadm/install/">https://docs.ceph.com/en/pacific/cephadm/install/</a></p>
</blockquote>
<p>各节点配置 hosts</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs conf">10.200.10.31 ceph-pro-1-10-200-10-31<br>10.200.10.32 ceph-pro-2-10-200-10-32<br>10.200.10.33 ceph-pro-3-10-200-10-33<br>10.200.10.34 ceph-pro-4-10-200-10-34<br>10.200.10.35 ceph-pro-5-10-200-10-35<br></code></pre></td></tr></table></figure>

<h2 id="3-安装-CEPHADM-所有节点"><a href="#3-安装-CEPHADM-所有节点" class="headerlink" title="3.安装 CEPHADM(所有节点)"></a>3.安装 CEPHADM(所有节点)</h2><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment">#下载cephadm</span><br>curl --remote-name --location https://hub.shutcm.cf/ceph/ceph/raw/pacific/src/cephadm/cephadm<br><span class="hljs-built_in">chmod</span> +x cephadm<br><span class="hljs-comment">#设置源</span><br>./cephadm add-repo --release pacific<br>sed -i <span class="hljs-string">&#x27;s#https://download.ceph.com#https://mirrors.aliyun.com/ceph#g&#x27;</span> /etc/apt/sources.list.d/ceph.list<br>apt-get update<br><span class="hljs-comment">#安装cephadm命令</span><br>./cephadm install<br><span class="hljs-comment">#确认在PATH中</span><br><span class="hljs-built_in">which</span> cephadm<br><span class="hljs-built_in">rm</span> -fr cephadm<br><span class="hljs-comment">#安装ceph命令</span><br>cephadm install ceph-common<br></code></pre></td></tr></table></figure>

<h2 id="4-引导新群集-第一台"><a href="#4-引导新群集-第一台" class="headerlink" title="4.引导新群集(第一台)"></a>4.引导新群集(第一台)</h2><blockquote>
<p>创建新的 Ceph 集群的第一步是在 Ceph 集群的第一台主机上运行 cephadm bootstrap 命令，运行此命令的操作将创建 Ceph 集群的第一个“监视程序守护程序”。</p>
</blockquote>
<p>而该监视程序守护程序需要一个 IP 地址，必须将 Ceph 集群的第一个主机的 IP 地址传递给 ceph bootstrap 命令。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">cephadm bootstrap --mon-ip 10.200.10.31<br><span class="hljs-comment">#设置addr</span><br><span class="hljs-comment">#ceph orch host set-addr ceph1 192.168.200.128</span><br><span class="hljs-comment">#检查</span><br>ceph orch host <span class="hljs-built_in">ls</span><br>ceph -s<br></code></pre></td></tr></table></figure>

<p>此命令将会：</p>
<figure class="highlight awk"><table><tr><td class="code"><pre><code class="hljs awk"><span class="hljs-number">1</span>) 在本地主机上为新集群创建监视和管理器守护程序<br><span class="hljs-number">2</span>) 为Ceph集群生成一个新的SSH密钥，并将其添加到root用户的<span class="hljs-regexp">/root/</span>.ssh/authorized_keys文件中<br><span class="hljs-number">3</span>) 将最小配置文件写入<span class="hljs-regexp">/etc/</span>ceph/ceph.conf<br><span class="hljs-number">4</span>) 将client.admin管理特权密钥写入<span class="hljs-regexp">/etc/</span>ceph/ceph.client.admin.keyring<br><span class="hljs-number">5</span>) 将公钥写入<span class="hljs-regexp">/etc/</span>ceph/ceph.pub<br></code></pre></td></tr></table></figure>

<h2 id="5-向群集添加主机"><a href="#5-向群集添加主机" class="headerlink" title="5.向群集添加主机"></a>5.向群集添加主机</h2><blockquote>
<p>在解析主机名等方面，cephadm 的要求非常低, 通过以下命令可以明确 IP 与主机名之间的解析：ceph orch host add</p>
</blockquote>
<p><strong>注意</strong>：添加主机时会自动创建 mon 服务，先按照 禁用监视器自动部署( 5.4 (1) ) 或 调整默认值监视器数量 (5.2 节)，再添加主机。</p>
<p>在新主机 root 用户的 authorized_keys 文件中安装集群的公共 SSH 密钥</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment">#ssh-copy-id -f -i /etc/ceph/ceph.pub root@*&lt;new-host&gt;*</span><br>ssh-copy-id -f -i /etc/ceph/ceph.pub root@ceph-pro-1-10-200-10-31<br>ssh-copy-id -f -i /etc/ceph/ceph.pub root@ceph-pro-2-10-200-10-32<br>ssh-copy-id -f -i /etc/ceph/ceph.pub root@ceph-pro-3-10-200-10-33<br>ssh-copy-id -f -i /etc/ceph/ceph.pub root@ceph-pro-4-10-200-10-34<br>ssh-copy-id -f -i /etc/ceph/ceph.pub root@ceph-pro-5-10-200-10-35<br></code></pre></td></tr></table></figure>

<p>添加方式两种：</p>
<ul>
<li>命令方式添加</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment">#ceph orch host add *&lt;newhost&gt;* [*&lt;ip&gt;*] [*&lt;label1&gt; ...*]</span><br>ceph orch host add ceph-pro-2-10-200-10-32 10.200.10.32 --labels _admin<br></code></pre></td></tr></table></figure>

<ul>
<li>yaml 方式添加</li>
</ul>
<p><code>host.yml</code></p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><code class="hljs yaml"><span class="hljs-meta">---</span><br><span class="hljs-attr">service_type:</span> <span class="hljs-string">host</span><br><span class="hljs-attr">addr:</span> <span class="hljs-number">192.168</span><span class="hljs-number">.200</span><span class="hljs-number">.131</span><br><span class="hljs-attr">hostname:</span> <span class="hljs-string">ceph2</span><br><span class="hljs-attr">labels:</span><br>  <span class="hljs-bullet">-</span> <span class="hljs-string">mon</span><br><span class="hljs-meta">---</span><br><span class="hljs-attr">service_type:</span> <span class="hljs-string">host</span><br><span class="hljs-attr">addr:</span> <span class="hljs-number">192.168</span><span class="hljs-number">.200</span><span class="hljs-number">.132</span><br><span class="hljs-attr">hostname:</span> <span class="hljs-string">ceph3</span><br><span class="hljs-attr">labels:</span><br>  <span class="hljs-bullet">-</span> <span class="hljs-string">mon</span><br></code></pre></td></tr></table></figure>

<h2 id="3-检查状态"><a href="#3-检查状态" class="headerlink" title="3.检查状态"></a>3.检查状态</h2><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">ceph orch host <span class="hljs-built_in">ls</span><br>ceph -s<br></code></pre></td></tr></table></figure>

<h2 id="6-部署-OSD"><a href="#6-部署-OSD" class="headerlink" title="6.部署 OSD"></a>6.部署 OSD</h2><p><strong><a href="https://docs.ceph.com/en/pacific/cephadm/services/osd/#">https://docs.ceph.com/en/pacific/cephadm/services/osd/#</a></strong></p>
<blockquote>
<p>当有新的 osd 加入集群或者移除了 osd，就会把状态上报给 Monitor，Monitor 知道了 osd map 发生了变化就会触发 rebalancing，确保 pg 能够平滑的移动到各个 osd 上，以 pg 为整体进行数据重平衡，重平衡的过程中可能会影响性能，一次性加入的 osd 越多，数据 rebalancing 就越频繁。</p>
</blockquote>
<blockquote>
<p>当在做 rebalance 的时候，每个 osd 都会按照 osd_max_backfills 指定数量的线程来同步，如果该数值比较大，同步会比较快，但是会影响部分性能；为了避免 rebalance 带来的性能影响，可以对 rebalance 进行关闭；添加完 osd 后再打开。</p>
</blockquote>
<h3 id="1-rebalance-关闭"><a href="#1-rebalance-关闭" class="headerlink" title="1).rebalance 关闭"></a>1).rebalance 关闭</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 设置标志位</span><br>ceph osd <span class="hljs-built_in">set</span> norebalance<br><br><span class="hljs-comment"># 关闭数据填充</span><br>ceph osd <span class="hljs-built_in">set</span> nobackfill<br><br><span class="hljs-comment"># 查看集群状态</span><br>ceph -s<br>  cluster:<br>    <span class="hljs-built_in">id</span>:     87cdd3b2-987b-11eb-989e-000c29b12ae1<br>    health: HEALTH_WARN<br>            nobackfill,norebalance flag(s) <span class="hljs-built_in">set</span>  <span class="hljs-comment"># 有此信息</span><br></code></pre></td></tr></table></figure>

<h3 id="2-rebalance-开启"><a href="#2-rebalance-开启" class="headerlink" title="2).rebalance 开启"></a>2).rebalance 开启</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment">#开启数据填充</span><br>ceph osd <span class="hljs-built_in">unset</span> nobackfill<br><span class="hljs-comment">#开启rebalance</span><br>ceph osd <span class="hljs-built_in">unset</span> norebalance<br><span class="hljs-comment"># 查看集群状态</span><br>ceph -s<br>  cluster:<br>    <span class="hljs-built_in">id</span>:     87cdd3b2-987b-11eb-989e-000c29b12ae1<br>    health: HEALTH_OK<br></code></pre></td></tr></table></figure>

<h3 id="3-列出节点可用设备"><a href="#3-列出节点可用设备" class="headerlink" title="3).列出节点可用设备"></a>3).列出节点可用设备</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment">#ceph orch device ls [--wide]</span><br>ceph orch device <span class="hljs-built_in">ls</span> --wide<br></code></pre></td></tr></table></figure>

<p>如果满足以下所有条件，则认为存储设备可用</p>
<figure class="highlight apache"><table><tr><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">1</span>) 设备必须没有分区<br><span class="hljs-attribute">2</span>) 设备不得具有任何LVM状态<br><span class="hljs-attribute">3</span>) 设备不得挂载<br><span class="hljs-attribute">4</span>) 设备不得包含文件系统<br><span class="hljs-attribute">5</span>) 设备不得包含Ceph BlueStore OSD<br><span class="hljs-attribute">6</span>) 设备必须大于<span class="hljs-number">5</span> GB<br></code></pre></td></tr></table></figure>

<h3 id="4-创建-osd"><a href="#4-创建-osd" class="headerlink" title="4).创建 osd"></a>4).创建 osd</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment">#需要至少 3 个 Ceph OSD 以实现冗余和高可用性</span><br>ceph orch daemon add osd ceph-pro-1-10-200-10-31:/dev/sdb<br>ceph orch daemon add osd ceph-pro-2-10-200-10-32:/dev/sdb<br>ceph orch daemon add osd ceph-pro-3-10-200-10-33:/dev/sdb<br></code></pre></td></tr></table></figure>

<h2 id="7-集群高可用"><a href="#7-集群高可用" class="headerlink" title="7.集群高可用"></a>7.集群高可用</h2><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment">#需要至少三个监视器才能实现冗余和高可用性</span><br>ceph orch apply mon 3<br><span class="hljs-comment"># 部署mon到指定节点</span><br>ceph orch host label add *&lt;host&gt;* mon<br><span class="hljs-comment">#至少需要两个管理器才能实现高可用性</span><br>ceph orch apply mgr 3<br><span class="hljs-comment">#</span><br></code></pre></td></tr></table></figure>

<h2 id="8-CephFS-部署"><a href="#8-CephFS-部署" class="headerlink" title="8.CephFS 部署"></a>8.CephFS 部署</h2><h2 id="9-部署-RGW"><a href="#9-部署-RGW" class="headerlink" title="9.部署 RGW"></a>9.部署 RGW</h2><p><a href="https://docs.ceph.com/en/pacific/cephadm/services/rgw/">https://docs.ceph.com/en/pacific/cephadm/services/rgw/</a></p>
<h1 id="三、运维"><a href="#三、运维" class="headerlink" title="三、运维"></a>三、运维</h1><p><a href="https://www.cnblogs.com/royaljames/p/9807532.html">https://www.cnblogs.com/royaljames/p/9807532.html</a></p>
<h2 id="1-向群集添加主机"><a href="#1-向群集添加主机" class="headerlink" title="1.向群集添加主机"></a>1.向群集添加主机</h2><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment">#1.在新主机的根用户文件中安装群集的公共 SSH 密钥</span><br>ssh-copy-id -f -i /etc/ceph/ceph.pub root@host2<br><span class="hljs-comment">#2.告诉 Ceph 新节点是群集的一部分：</span><br>ceph orch host add host2<br></code></pre></td></tr></table></figure>

<h2 id="2-部署其他监视器-monitor"><a href="#2-部署其他监视器-monitor" class="headerlink" title="2.部署其他监视器(monitor)"></a>2.部署其他监视器(monitor)</h2><blockquote>
<p>典型的 Ceph 群集具有分布在不同主机的三个或五个监视器守护程序。如果群集中有五个或更多节点，我们建议部署五个监视器。</p>
</blockquote>
<p>当 Ceph 知道监视器应该使用什么 IP 子网时，它可以随着群集的增长（或收缩）自动部署和缩放监视器。默认情况下，Ceph 假定其他监视器应使用与第一个监视器的 IP 相同的子网。</p>
<p>如果您的 Ceph 监视器（或整个群集）住在单个子网中，则在向群集添加新主机时，默认情况下，cephadm 会自动添加多达 5 个监视器。无需进一步步骤。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment">#如果有特定的 IP 子网应该由监视器使用，您可以用CIDR格式（例如 ） 配置该子网，Cephadm 仅在配置的子网中配置了 IP 的主机上部署新的监视器守护程序。</span><br>ceph config <span class="hljs-built_in">set</span> mon public_network 10.1.2.0/24<br><span class="hljs-comment">#如果要调整 5 个监视器的默认值：</span><br>ceph orch apply mon *&lt;number-of-monitors&gt;*<br><span class="hljs-comment">#若要在一组特定的主机上部署监视器，请确保在此列表中包括第一个（引导）主机。</span><br>ceph orch apply mon *&lt;host1,host2,host3,...&gt;*<br><span class="hljs-comment">#您可以通过使用主机标签来控制监视器运行的主机。要将标签设置为相应的主机，请：mon</span><br>ceph orch host label add *&lt;hostname&gt;* mon<br><span class="hljs-comment">#要查看当前主机和标签：</span><br>ceph orch host <span class="hljs-built_in">ls</span><br><span class="hljs-comment">#例如：</span><br><span class="hljs-comment"># ceph orch host label add host1 mon</span><br><span class="hljs-comment"># ceph orch host label add host2 mon</span><br><span class="hljs-comment"># ceph orch host label add host3 mon</span><br><span class="hljs-comment"># ceph orch host ls</span><br>HOST   ADDR   LABELS  STATUS<br>host1         mon<br>host2         mon<br>host3         mon<br>host4<br>host5<br><span class="hljs-comment">#告诉 cephadm 根据标签部署监视器：</span><br>ceph orch apply mon label:mon<br><span class="hljs-comment">#您可以显式指定每个监视器的 IP 地址或 CIDR 网络，并控制其放置位置。要禁用自动监视器部署：</span><br>ceph orch apply mon --unmanaged<br><span class="hljs-comment">#要部署每个附加监视器：</span><br>ceph orch daemon add mon *&lt;host1:ip-or-network1&gt; [&lt;host1:ip-or-network-2&gt;...]*<br><span class="hljs-comment">#例如，要在使用 IP 地址上部署第二个监视器，在网络上部署第三个监视器</span><br><span class="hljs-comment"># ceph orch apply mon --unmanaged</span><br><span class="hljs-comment"># ceph orch daemon add mon newhost1:10.1.2.123</span><br><span class="hljs-comment"># ceph orch daemon add mon newhost2:10.1.2.0/24</span><br><br><span class="hljs-comment">#若要确保监视器应用于这三台主机中的每一个主机，请运行以下命令：</span><br>ceph orch apply mon <span class="hljs-string">&quot;host1,host2,host3&quot;</span><br></code></pre></td></tr></table></figure>

<h1 id="使用-YAML-规范"><a href="#使用-YAML-规范" class="headerlink" title="使用 YAML 规范"></a>使用 YAML 规范</h1><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">ceph orch apply -i file.yaml<br></code></pre></td></tr></table></figure>

<figure class="highlight yaml"><table><tr><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">service_type:</span> <span class="hljs-string">mon</span><br><span class="hljs-attr">placement:</span><br>  <span class="hljs-attr">hosts:</span><br>    <span class="hljs-bullet">-</span> <span class="hljs-string">host1</span><br>    <span class="hljs-bullet">-</span> <span class="hljs-string">host2</span><br>    <span class="hljs-bullet">-</span> <span class="hljs-string">host3</span><br></code></pre></td></tr></table></figure>

<h2 id="3-部署-OSD"><a href="#3-部署-OSD" class="headerlink" title="3.部署 OSD"></a>3.部署 OSD</h2><h3 id="1-所有群集主机上的存储设备清单可以显示"><a href="#1-所有群集主机上的存储设备清单可以显示" class="headerlink" title="1.所有群集主机上的存储设备清单可以显示"></a>1.所有群集主机上的存储设备清单可以显示</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">ceph orch device <span class="hljs-built_in">ls</span><br></code></pre></td></tr></table></figure>

<ul>
<li>设备必须没有分区。</li>
<li>设备不得具有任何 LVM 状态。</li>
<li>不得安装设备。</li>
<li>设备不能包含文件系统。</li>
<li>设备不得包含 Ceph BlueStore OSD。</li>
<li>设备必须大于 5 GB。</li>
</ul>
<h3 id="2-创建新-OSD-的方法"><a href="#2-创建新-OSD-的方法" class="headerlink" title="2.创建新 OSD 的方法"></a>2.创建新 OSD 的方法</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 1.告诉 Ceph 使用任何可用和未使用的存储设备：</span><br>ceph orch apply osd --all-available-devices<br><span class="hljs-comment"># 2.从特定主机上的特定设备创建 OSD：ceph orch daemon add osd *&lt;host&gt;*:*&lt;device-path&gt;*</span><br>ceph orch daemon add osd host1:/dev/sdb<br><span class="hljs-comment"># 3.使用OSD 服务规范描述设备，根据设备属性、此类设备类型（SSD 或 HDD）、设备型号名称、大小或设备存在的主机使用：</span><br>ceph orch apply osd -i spec.yml<br></code></pre></td></tr></table></figure>

<h2 id="4-部署-MDS"><a href="#4-部署-MDS" class="headerlink" title="4.部署 MDS"></a>4.部署 MDS</h2><p>使用 CephFS 文件系统需要一个或多个 MDS 守护程序。如果使用较新的接口创建新文件系统，则会自动创建这些接口。有关详细信息，请参阅 FS 卷和子卷。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">ceph orch apply mds *&lt;fs-name&gt;* --placement=<span class="hljs-string">&quot;*&lt;num-daemons&gt;* [*&lt;host1&gt;* ...]&quot;</span><br></code></pre></td></tr></table></figure>

<h2 id="5-部署-RGW"><a href="#5-部署-RGW" class="headerlink" title="5.部署 RGW"></a>5.部署 RGW</h2><h2 id="6-管理-Monitor-map"><a href="#6-管理-Monitor-map" class="headerlink" title="6.管理 Monitor map"></a>6.管理 Monitor map</h2><h3 id="1-多-Monitor-同步机制"><a href="#1-多-Monitor-同步机制" class="headerlink" title="1).多 Monitor 同步机制"></a>1).多 Monitor 同步机制</h3><blockquote>
<p>在生产环境建议最少三节点 monitor，以确保 cluster map 的高可用性和冗余性,monitor 节点不应该过多甚至操作 9 节点的行为,会导致数据读写时间下降，影响系统集群的性能。</p>
</blockquote>
<ul>
<li>monitor 使用 paxos 算法作为集群状态上达成一致的机制。paxos 是一种分布式一致性算法。每当 monitor 修改 map 时，它会通过 paxos 发送更新到其他 monitor。Ceph 只有在大多数 monitor 就更新达成一致时提交 map 的新版本。</li>
<li>cluster map 的更新操作需要 Paxos 确认，但是读操作不经由 Paxos，而是直接访问本地的 kv 存储。</li>
</ul>
<h3 id="2-Monitor-选举机制"><a href="#2-Monitor-选举机制" class="headerlink" title="2).Monitor 选举机制"></a>2).Monitor 选举机制</h3><ul>
<li>多个 monitor 之间需要建立仲裁并选择出一个 leader，其他节点则作为工作节点（peon）。</li>
<li>在选举完成并确定 leader 之后，leader 将从所有其他 monitor 请求最新的 map epoc，以确保 leader 具有集群的最新视图。</li>
<li>要维护 monitor 集群的正常工作，必须有超过半数的节点正常。</li>
</ul>
<h3 id="3-Monitor-租约"><a href="#3-Monitor-租约" class="headerlink" title="3).Monitor 租约"></a>3).Monitor 租约</h3><ul>
<li>在 Monitor 建立仲裁后，leader 开始分发短期的租约到所有的 monitors。让它们能够分发 cluster map 到 OSD 和 client。</li>
<li>Monitor 租约默认每 3s 续期一次。</li>
<li>当 peon monitor 没有确认它收到租约时，leader 假定该 monitor 异常，它会召集新的选举以建立仲裁。</li>
<li>如果 peon monitor 的租约到期后没有收到 leader 的续期，它会假定 leader 异常，并会召集新的选举。</li>
<li>所以如果生产环境中存在多个 monitor 时候某个节点的超时会猝发节点的重新选举导致 client 无法第一时间拿到最新的 crushmap 图像也就无法去对应的 osd 上的 pv 写入数据了。</li>
</ul>
<h3 id="4-常用的-monitor-管理"><a href="#4-常用的-monitor-管理" class="headerlink" title="4).常用的 monitor 管理"></a>4).常用的 monitor 管理</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment">#打印monitor map信息</span><br>ceph mon dump<br><br><span class="hljs-comment">#将monitor map导出为一个二进制文件</span><br>ceph mon getmap -o ./monmap<br><br><span class="hljs-comment">#打印导出的二进制文件的内容</span><br>monmaptool --<span class="hljs-built_in">print</span> ./monmap<br><br><span class="hljs-comment">#修改二进制文件，从monmap删除某个monitor</span><br>monmaptool ./monmap --<span class="hljs-built_in">rm</span> &lt;<span class="hljs-built_in">id</span>&gt;<br><br><span class="hljs-comment">#修改二进制文件，往monmap中添加一个monitor</span><br>monmaptool ./monmap --add &lt;<span class="hljs-built_in">id</span>&gt; &lt;ip:port&gt;<br><br><span class="hljs-comment">#导入一个二进制文件，在导入之前，需要先停止monitor</span><br>ceph-mon -i &lt;<span class="hljs-built_in">id</span>&gt; --inject-monmap ./monmap<br></code></pre></td></tr></table></figure>

<h2 id="7-管理-OSD-Map"><a href="#7-管理-OSD-Map" class="headerlink" title="7.管理 OSD Map"></a>7.管理 OSD Map</h2><ul>
<li>每当 OSD 加入或离开集群时，Ceph 都会更新 OSD map。</li>
<li>OSD 不使用 leader 来管理 OSD map，它们会在自身之间传播同步 map。OSD 会利用 OSD map epoch 标记它们交换的每一条信息，当 OSD 检测到自己已落后时，它会使用其对等 OSD 执行 map 更新。</li>
<li>在大型集群中 OSD map 更新会非常频繁，节点会执行递增 map 更新。</li>
<li>Ceph 也会利用 epoch 来标记 OSD 和 client 之间的消息。当 client 连接到 OSD 时 OSD 会检查 epoch。如果发现 epoch 不匹配，则 OSD 会以正确的 epoch 响应，以便客户端可以更新其 OSD map。</li>
<li>OSD 定期向 monitor 报告自己的状态，OSD 之间会交换心跳，以便检测对等点的故障，并报告给 monitor。</li>
<li>leader monitor 发现 OSD 故障时，它会更新 map，递增 epoch，并使用 Paxos 更新协议来通知其他 monitor，同时撤销租约，并发布新的租约，以使 monitor 以分发最新的 OSD map。</li>
</ul>
<h3 id="1-OSD-状态解读"><a href="#1-OSD-状态解读" class="headerlink" title="1).OSD 状态解读"></a>1).OSD 状态解读</h3><ul>
<li>1.正常状态的 OSD 为 up 且 in</li>
<li>2.当 OSD 故障时，守护进程 offline，在 5 分钟内，集群仍会将其标记为 up 和 in，这是为了防止网络抖动</li>
<li>3.如果 5 分钟内仍未恢复，则会标记为 down 和 out。此时该 OSD 上的 PG 开始迁移。这个 5 分钟的时间间隔可以通过 mon_osd_down_out_interval 配置项修改</li>
<li>4.当故障的 OSD 重新上线以后，会触发新的数据再平衡</li>
<li>5.当集群有 noout 标志位时，则 osd 下线不会导致数据恢复</li>
<li>6.OSD 每隔 6s 会互相验证状态。并每隔 120s 向 mon 报告一次状态。</li>
</ul>
<h3 id="2-OSD-map-命令"><a href="#2-OSD-map-命令" class="headerlink" title="2).OSD map 命令"></a>2).OSD map 命令</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">ceph osd dump<br>ceph osd getmap -o binfile<br>osdmaptool --<span class="hljs-built_in">print</span> binfile<br>osdmaptool --export-crush crushbinfile binfile<br>osdmaptool --import-crush crushbinfile binfile<br>osdmaptool --test-map-pg pgid binfile<br></code></pre></td></tr></table></figure>

<h3 id="3-OSD-的状态"><a href="#3-OSD-的状态" class="headerlink" title="3.)OSD 的状态"></a>3.)OSD 的状态</h3><ul>
<li><p>OSD 运行状态</p>
<ul>
<li>up</li>
<li>down</li>
<li>out</li>
<li>in</li>
</ul>
</li>
<li><p>OSD 容量状态</p>
<ul>
<li>nearfull</li>
<li>full</li>
</ul>
</li>
</ul>
<p>常用指令</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment">#显示OSD状态</span><br>ceph osd <span class="hljs-built_in">stat</span><br><br><span class="hljs-comment">#报告osd使用量</span><br>ceph osd <span class="hljs-built_in">df</span><br><br><span class="hljs-comment">#查找指定osd位置</span><br>ceph osd find<br></code></pre></td></tr></table></figure>

<h3 id="4-OSD-容量"><a href="#4-OSD-容量" class="headerlink" title="4.)OSD 容量"></a>4.)OSD 容量</h3><ul>
<li>当集群容量达到 mon_osd_nearfull_ratio 的值时，集群会进入 HEALTH_WARN 状态。这是为了在达到 full_ratio 之前，提醒添加 OSD。默认设置为 0.85，即 85%</li>
<li>当集群容量达到 mon_osd_full_ratio 的值时，集群将停止写入，但允许读取。集群会进入到 HEALTH_ERR 状态。默认为 0.95，即 95%。这是为了防止当一个或多个 OSD 故障时仍留有余地能重平衡数据</li>
</ul>
<p>设置方法：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">ceph osd set-full-ratio 0.95<br>ceph osd set-nearfull-ratio 0.85<br>ceph osd dump<br></code></pre></td></tr></table></figure>

<h3 id="5-OSD-状态参数"><a href="#5-OSD-状态参数" class="headerlink" title="5).OSD 状态参数"></a>5).OSD 状态参数</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># osd之间传递心跳的间隔时间</span><br>osd_heartbeat_interval<br><br><span class="hljs-comment"># 一个osd多久没心跳，就会被集群认为它down了</span><br>osd_heartbeat_grace<br><br><span class="hljs-comment"># 确定一个osd状态为down的最少报告来源osd数</span><br>mon_osd_min_down_reporters<br><br><span class="hljs-comment"># 一个OSD必须重复报告一个osd状态为down的次数</span><br>mon_osd_min_down_reports<br><br><span class="hljs-comment"># 当osd停止响应多长时间，将其标记为down和out</span><br>mon_osd_down_out_interval<br><br><span class="hljs-comment"># monitor宣布失败osd为down前的等待时间</span><br>mon_osd_report_timeout<br><br><span class="hljs-comment"># 一个新的osd加入集群时，等待多长时间，开始向monitor报告</span><br>osd_mon_report_interval_min<br><br><span class="hljs-comment"># monitor允许osd报告的最大间隔，超时就认为它down了</span><br>osd_mon_report_interval_max<br><br><span class="hljs-comment"># osd向monitor报告心跳的时间</span><br>osd_mon_heartbeat_interval<br></code></pre></td></tr></table></figure>

<h2 id="8-管理-PG"><a href="#8-管理-PG" class="headerlink" title="8.管理 PG"></a>8.管理 PG</h2><h3 id="1-管理文件到-PG-映射"><a href="#1-管理文件到-PG-映射" class="headerlink" title="1).管理文件到 PG 映射"></a>1).管理文件到 PG 映射</h3><p>test 对象所在 pg id 为 10.35，存储在三个 osd 上，分别为 osd.2、osd.1 和 osd.8，其中 osd.2 为 primary osd</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">rados -p <span class="hljs-built_in">test</span> put <span class="hljs-built_in">test</span> /etc/ceph/ceph.conf<br>ceph osd map <span class="hljs-built_in">test</span> <span class="hljs-built_in">test</span><br>    osdmap e201 pool <span class="hljs-string">&#x27;test&#x27;</span> (10) object <span class="hljs-string">&#x27;test&#x27;</span> -&gt; pg 10.40e8aab5 (10.35) -&gt; up ([2,1,8], p2) acting ([2,1,8], p2)<br><br><span class="hljs-comment">#处于up状态的osd会一直留在PG的up set和acting set中，一旦主osd down，它首先会从up set中移除</span><br><span class="hljs-comment">#然后从acting set中移除，之后从OSD将被升级为主。Ceph会将故障OSD上的PG恢复到一个新OSD上</span><br><span class="hljs-comment">#然后再将这个新OSD加入到up和acting set中来维持集群的高可用性</span><br></code></pre></td></tr></table></figure>

<h3 id="2-管理-struck-状态的-PG"><a href="#2-管理-struck-状态的-PG" class="headerlink" title="2).管理 struck 状态的 PG"></a>2).管理 struck 状态的 PG</h3><ul>
<li><p>如果 PG 长时间（mon_pg_stuck_threshold，默认为 300s）出现如下状态时，MON 会将该 PG 标记为 stuck：</p>
<ul>
<li>inactive：pg 有 peering 问题</li>
<li>unclean：pg 在故障恢复时遇到问题</li>
<li>stale：pg 没有任何 OSD 报告，可能其所有的 OSD 都是 down 和 out</li>
<li>undersized：pg 没有充足的 osd 来存储它应具有的副本数</li>
</ul>
</li>
<li><p>默认情况下，Ceph 会自动执行恢复，但如果未能自动恢复，则集群状态会一直处于 HEALTH_WARN 或者 HEALTH_ERR</p>
</li>
<li><p>如果特定 PG 的所有 osd 都是 down 和 out 状态，则 PG 会被标记为 stale。要解决这一情况，其中一个 OSD 必须要重生，且具有可用的 PG 副本，否则 PG 不可用</p>
</li>
<li><p>Ceph 可以声明 osd 或 PG 已丢失，这也就意味着数据丢失。需要说明的是，osd 的运行离不开 journal，如果 journal 丢失，则 osd 停止</p>
</li>
</ul>
<h3 id="3-struck-状态操作"><a href="#3-struck-状态操作" class="headerlink" title="3).struck 状态操作"></a>3).struck 状态操作</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 检查处于stuck状态的pg</span><br>ceph pg dump_stuck<br><span class="hljs-comment"># 检查导致pg一致阻塞在peering状态的osd</span><br>ceph osd blocked-by<br><span class="hljs-comment"># 检查某个pg的状态</span><br>ceph pg dump all|grep pgid<br><span class="hljs-comment"># 声明pg丢失</span><br>ceph pg pgid mark_unfound_lost revert|delete<br><span class="hljs-comment"># 声明osd丢失（需要osd状态为down且out）</span><br>ceph osd lost osdid --yes-i-really-mean-it<br></code></pre></td></tr></table></figure>

<h3 id="4-手动控制-PG-的-Primary-OSD"><a href="#4-手动控制-PG-的-Primary-OSD" class="headerlink" title="4).手动控制 PG 的 Primary OSD"></a>4).手动控制 PG 的 Primary OSD</h3><p>可以通过手动修改 osd 的权重以提升 特定 OSD 被选为 PG Primary OSD 的概率，避免将速度慢的磁盘用作 primary osd。</p>
<p>需要先在配置文件中配置如下参数：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">mon_osd_allow_primary_affinity = <span class="hljs-literal">true</span><br></code></pre></td></tr></table></figure>

<h3 id="5-调整权重示例"><a href="#5-调整权重示例" class="headerlink" title="5).调整权重示例"></a>5).调整权重示例</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">1. 查看现在有多少PG的主OSD是osd.0<br>ceph pg dump |grep active+clean |egrep <span class="hljs-string">&quot;\[0,&quot;</span> |<span class="hljs-built_in">wc</span> -l<br><br>2. 修改osd.0的权重<br>ceph osd primary-affinity osd.0 0  <span class="hljs-comment"># 权重范围从0.0到1.0</span><br><br>3. 再次查看现在有多少PG的主OSD是osd.0<br>ceph pg dump |grep active+clean |egrep <span class="hljs-string">&quot;\[0,&quot;</span> |<span class="hljs-built_in">wc</span> -l<br></code></pre></td></tr></table></figure>

<h2 id="9-Pool-存储池-管理"><a href="#9-Pool-存储池-管理" class="headerlink" title="9.Pool(存储池)管理"></a>9.Pool(存储池)管理</h2><p><a href="https://blog.csdn.net/weixin_42440345/article/details/81118964">https://blog.csdn.net/weixin_42440345&#x2F;article&#x2F;details&#x2F;81118964</a></p>
<h3 id="PG-和-PGP-的区别"><a href="#PG-和-PGP-的区别" class="headerlink" title="PG 和 PGP 的区别"></a>PG 和 PGP 的区别</h3><p><a href="https://www.cnblogs.com/zphj1987/p/13575377.html">https://www.cnblogs.com/zphj1987/p/13575377.html</a></p>
<p>PG 是指定存储池存储对象的目录有多少个，PGP 是存储池 PG 的 OSD 分布组合个数</p>
<p>PG 的增加会引起 PG 内的数据进行分裂，分裂到相同的 OSD 上新生成的 PG 当中</p>
<p>PGP 的增加会引起部分 PG 的分布进行变化，但是不会引起 PG 内对象的变动</p>
<h3 id="4-限制-pool-配置更改"><a href="#4-限制-pool-配置更改" class="headerlink" title="4.)限制 pool 配置更改"></a>4.)限制 pool 配置更改</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment">#禁止池被删除</span><br>osd_pool_default_flag_nodelete<br><br><span class="hljs-comment">#禁止池的pg_num和pgp_num被修改</span><br>osd_pool_default_flag_nopgchange<br><br><span class="hljs-comment">#禁止修改池的size和min_size</span><br>osd_pool_default_flag_nosizechange<br></code></pre></td></tr></table></figure>

<h3 id="1-查看-pool"><a href="#1-查看-pool" class="headerlink" title="1.查看 pool"></a>1.查看 pool</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment">#查看所有pool</span><br>ceph osd lspools<br><span class="hljs-comment">#获取集群内所有POOL的概况信息,集群内POOL的个数、对应的POOL id、POOL名称、副本数、最小副本数，ruleset及POOL snap等信息</span><br>ceph osd pool <span class="hljs-built_in">ls</span> detail<br><span class="hljs-comment">#查看POOL的统计信息</span><br></code></pre></td></tr></table></figure>

<h3 id="2-创建-pool"><a href="#2-创建-pool" class="headerlink" title="2.创建 pool"></a>2.创建 pool</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment">#创建一个副本类型的POOL</span><br>ceph osd pool create &#123;pool-name&#125; &#123;pg-num&#125; [&#123;pgp-num&#125;] [&#123;pgp-num&#125;] [replicated] [ruleset]<br><span class="hljs-comment">#举例：</span><br>ceph osd pool create rbd  32 32<br><span class="hljs-comment">#创建一个纠删码类型的POOL</span><br>ceph osd pool create &#123;pool-name&#125; &#123;pg-num&#125; &#123;pgp-num&#125; erasure [erasure-code-profile] [ruleset]<br></code></pre></td></tr></table></figure>

<p>在<code>&#123;&#125;</code>内的参数为必选项,<code>[]</code>内的参数均设有默认值,如果没有更改设计,可以不添加。</p>
<p>参数的含义如下:</p>
<ul>
<li><strong>pool-name</strong>: POOL 的名字；必须添加。</li>
<li><strong>pg-num</strong>: POOL 拥有的 PG 总数；必须添加。</li>
<li><strong>pgp-num</strong>: POOL 拥有的 PGP 总数；非必须添加。默认与 pg-num 相同。</li>
<li><strong>replicated|erasure</strong>: POOL 类型；非必须添加。如不指定为 erasure,则默认为 replicated 类型。</li>
<li><strong>ruleset</strong>: POOL 所用的 CRUSH 规则 ID。非必须添加。默认为 0,若需指定其他 ruleset,需确保 ruleset 必须存在。</li>
<li><strong>erasure-code-profile</strong>: 仅用于纠删码类型的 POOL。指定纠删码配置框架,此配置必须已由 osd erasure-code-profile set 定义</li>
</ul>
<p>这里强制选择 pg_num 和 pgp_num，因为 ceph 集群不能自动计算 pg 数量。下面有一些官方建议的 pg 使用数量：</p>
<ul>
<li>小于 5 个 osd 设置 pg_num 为 128</li>
<li>5 到 10 个 osd 设置 pg_num 为 512</li>
<li>10 到 50 个 osd 设置 pg_num 为 1024</li>
<li>如果超过 50 个 osd 你需要自己明白权衡点，并且能自行计算 pg_num 的数量</li>
</ul>
<p>pg_num 通用计算方法:</p>
<p>(OSDs * 100)</p>
<p>Total PGs &#x3D;  ————</p>
<p>pool size</p>
<h3 id="3-修改-pool"><a href="#3-修改-pool" class="headerlink" title="3.修改 pool"></a>3.修改 pool</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">ceph osd pool <span class="hljs-built_in">set</span> &#123;pool-name&#125; &#123;key&#125; &#123;value&#125;<br>size：设置存储池中的对象副本数，详情参见设置对象副本数。仅适用于副本存储池。<br>min_size：设置 I/O 需要的最小副本数，详情参见设置对象副本数。仅适用于副本存储池。<br>pg_num：计算数据分布时的有效 PG 数。只能大于当前 PG 数。<br>pgp_num：计算数据分布时使用的有效 PGP 数量。小于等于存储池的 PG 数。<br>hashpspool：给指定存储池设置/取消 HASHPSPOOL 标志。<br>target_max_bytes：达到 max_bytes 阀值时会触发 Ceph 冲洗或驱逐对象。<br>target_max_objects：达到 max_objects 阀值时会触发 Ceph 冲洗或驱逐对象。<br>scrub_min_interval：在负载低时，洗刷存储池的最小间隔秒数。如果是 0 ，就按照配置文件里的 osd_scrub_min_interval 。<br>scrub_max_interval：不管集群负载如何，都要洗刷存储池的最大间隔秒数。如果是 0 ，就按照配置文件里的 osd_scrub_max_interval 。<br>deep_scrub_interval：“深度”洗刷存储池的间隔秒数。如果是 0 ，就按照配置文件里的 osd_deep_scrub_interval 。<br></code></pre></td></tr></table></figure>

<h3 id="4-删除存储池"><a href="#4-删除存储池" class="headerlink" title="4.删除存储池"></a>4.删除存储池</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">ceph osd pool delete &#123;pool-name&#125; [&#123;pool-name&#125; --yes-i-really-really-mean-it]<br></code></pre></td></tr></table></figure>

<h3 id="5-重命名存储池"><a href="#5-重命名存储池" class="headerlink" title="5.重命名存储池"></a>5.重命名存储池</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">ceph osd pool rename &#123;current-pool-name&#125; &#123;new-pool-name&#125;<br></code></pre></td></tr></table></figure>

<h3 id="6-查看存储池统计信息"><a href="#6-查看存储池统计信息" class="headerlink" title="6.查看存储池统计信息"></a>6.查看存储池统计信息</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">rados <span class="hljs-built_in">df</span><br></code></pre></td></tr></table></figure>

<h3 id="7-给存储池做快照"><a href="#7-给存储池做快照" class="headerlink" title="7.给存储池做快照"></a>7.给存储池做快照</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">ceph osd pool mksnap &#123;pool-name&#125; &#123;snap-name&#125;<br></code></pre></td></tr></table></figure>

<h3 id="8-删除存储池的快照"><a href="#8-删除存储池的快照" class="headerlink" title="8.删除存储池的快照"></a>8.删除存储池的快照</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">ceph osd pool rmsnap &#123;pool-name&#125; &#123;snap-name&#125;<br></code></pre></td></tr></table></figure>

<h3 id="9-获取存储池选项值"><a href="#9-获取存储池选项值" class="headerlink" title="9.获取存储池选项值"></a>9.获取存储池选项值</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">ceph osd pool get &#123;pool-name&#125; &#123;key&#125;<br></code></pre></td></tr></table></figure>

<h3 id="10-获取对象副本数"><a href="#10-获取对象副本数" class="headerlink" title="10.获取对象副本数"></a>10.获取对象副本数</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">ceph osd dump | grep <span class="hljs-string">&#x27;replicated size&#x27;</span><br></code></pre></td></tr></table></figure>

<h3 id="11-设置存储池配额"><a href="#11-设置存储池配额" class="headerlink" title="11.设置存储池配额"></a>11.设置存储池配额</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">命令格式：<br><span class="hljs-comment"># ceph osd pool set-quota &#123;pool-name&#125; [max_objects &#123;obj-count&#125;] [max_bytes &#123;bytes&#125;]</span><br>命令举例：<br><span class="hljs-comment"># ceph osd pool set-quota rbd max_objects 10000</span><br></code></pre></td></tr></table></figure>

<h2 id="10-自定义-Crush-Map"><a href="#10-自定义-Crush-Map" class="headerlink" title="10.自定义 Crush Map"></a>10.自定义 Crush Map</h2><blockquote>
<p>crush map 决定了客户端数据最终写入的 osd 的位置，在某些情况下存在 hdd 和 ssd 两种盘想让某些数据写入到指定的 osd 中这个时候就是需要去人为的手动编译 crush-map，编辑要修改的部分，再导入集群中达到我们特定的目的</p>
</blockquote>
<h3 id="1-Crush-的放置策略"><a href="#1-Crush-的放置策略" class="headerlink" title="1).Crush 的放置策略"></a>1).Crush 的放置策略</h3><ul>
<li>Ceph 使用 CRUSH 算法（Controlled Replication Under Scalable Hashing 可扩展哈希下的受控复制）来计算哪些 OSD 存放哪些对象</li>
<li>对象分配到 PG 中，CRUSH 决定这些 PG 使用哪些 OSD 来存储对象。理想情况下，CRUSH 会将数据均匀的分布到存储中</li>
<li>当添加新 OSD 或者现有的 OSD 出现故障时，Ceph 使用 CRUSH 在活跃的 OSD 上重平衡数据 CRUSH map 是 CRUSH 算法的中央配置机制，可通过调整 CRUSHmap 来优化数据存放位置默认情况下，CRUSH 将一个对象的多个副本放置到不同主机上的 0SD 中。可以配置 CRUSH map 和 CRUSH rules，使一个对象的多个副本放置到不同房间或者不同机柜的主机上的 0SD 中。</li>
<li>也可以将 SSD 磁盘分配给需要高速存储的池</li>
</ul>
<h3 id="2-编译与翻译和更新"><a href="#2-编译与翻译和更新" class="headerlink" title="2).编译与翻译和更新"></a>2).编译与翻译和更新</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment">#导出CRUSH map</span><br>ceph osd getcrushmap -o ./crushmap.bin<br><span class="hljs-comment">#解译CRUSH map</span><br>crushtool -d ./crushmap.bin ./crushmap.txt<br><span class="hljs-comment">#修改后的CRUSH map重新编译</span><br>crushtool -c ./crushmap.txt-o ./crushmap-new.bin<br><span class="hljs-comment">#更新CRUSH map</span><br>ceph osd setcrushmap-i./crushmap-new.bin<br><span class="hljs-comment">#查询crush map的内容（返回json）</span><br>ceph osd crush dump<br></code></pre></td></tr></table></figure>

<p>例子</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">root default &#123;<br>    id-1           <span class="hljs-comment"># do not change unnecessarily</span><br>    id-2 class hdd <span class="hljs-comment">#do not change unnecessarily</span><br>    <span class="hljs-comment">#weiqht 0.166</span><br>    alg straw2<br>    <span class="hljs-built_in">hash</span> 0<span class="hljs-comment">#rjenkins1</span><br>    item rackl weight 0.055<br>    item rack2 weiqht 0.055<br>    item rack3 weight 0.055<br>&#125;<br><br><span class="hljs-comment">#rules</span><br>rule replicated rule&#123;<br>    <span class="hljs-built_in">id</span> 0<br>    <span class="hljs-built_in">type</span> replicated<br>    min size 1<br>    max size 10<br>    step take default  <span class="hljs-comment">#只要是应用这个rule的都把数据写入到defaults下</span><br>    step chooseleaf firstn 0 <span class="hljs-built_in">type</span> host  <span class="hljs-comment">#定义故障的故障域为物理集机器级别（rack为机柜级别）</span><br>    step emit <span class="hljs-comment">#结尾符号</span><br>&#125;<br></code></pre></td></tr></table></figure>

<h2 id="11-admin-sockets-管理守护进程"><a href="#11-admin-sockets-管理守护进程" class="headerlink" title="11.admin sockets 管理守护进程"></a>11.admin sockets 管理守护进程</h2><ul>
<li>通过 admin sockets，管理员可以直接与守护进程交互。如查看和修改守护进程的配置参数。</li>
<li>守护进程的 socket 文件一般是&#x2F;var&#x2F;run&#x2F;ceph&#x2F;cluster-cluster−type.$id.asok</li>
<li>基于 admin sockets 的操作：</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">ceph daemon <span class="hljs-variable">$type</span>.<span class="hljs-variable">$id</span> <span class="hljs-built_in">command</span><br><span class="hljs-comment">#或者</span><br>ceph --admin-daemon /var/run/ceph/<span class="hljs-variable">$cluster</span>-<span class="hljs-variable">$type</span>.<span class="hljs-variable">$id</span>.asok <span class="hljs-built_in">command</span><br><span class="hljs-comment">#常用command如下：</span><br><span class="hljs-built_in">help</span><br>config get parameter<br>config <span class="hljs-built_in">set</span> parameter<br>config show<br>perf dump<br></code></pre></td></tr></table></figure>

<h2 id="12-用户管理"><a href="#12-用户管理" class="headerlink" title="12.用户管理"></a>12.用户管理</h2><blockquote>
<p>Ceph 把数据以对象的形式存于各存储池中。Ceph 用户必须具有访问存储池的权限才能够读写数据。另外，Ceph 用户必须具有执行权限才能够使用 Ceph 的管理命令。</p>
</blockquote>
<h3 id="1、查看用户信息"><a href="#1、查看用户信息" class="headerlink" title="1、查看用户信息"></a>1、查看用户信息</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">查看所有用户信息<br><span class="hljs-comment"># ceph auth list</span><br>获取所有用户的key与权限相关信息<br><span class="hljs-comment"># ceph auth get client.admin</span><br>如果只需要某个用户的key信息，可以使用pring-key子命令<br><span class="hljs-comment"># ceph auth print-key client.admin</span><br></code></pre></td></tr></table></figure>

<h3 id="2、添加用户"><a href="#2、添加用户" class="headerlink" title="2、添加用户"></a>2、添加用户</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># ceph auth add client.john mon &#x27;allow r&#x27; osd &#x27;allow rw pool=liverpool&#x27;</span><br><span class="hljs-comment"># ceph auth get-or-create client.paul mon &#x27;allow r&#x27; osd &#x27;allow rw pool=liverpool&#x27;</span><br><span class="hljs-comment"># ceph auth get-or-create client.george mon &#x27;allow r&#x27; osd &#x27;allow rw pool=liverpool&#x27; -o george.keyring</span><br><span class="hljs-comment"># ceph auth get-or-create-key client.ringo mon &#x27;allow r&#x27; osd &#x27;allow rw pool=liverpool&#x27; -o ringo.key</span><br></code></pre></td></tr></table></figure>

<h3 id="3、修改用户权限"><a href="#3、修改用户权限" class="headerlink" title="3、修改用户权限"></a>3、修改用户权限</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># ceph auth caps client.john mon &#x27;allow r&#x27; osd &#x27;allow rw pool=liverpool&#x27;</span><br><span class="hljs-comment"># ceph auth caps client.paul mon &#x27;allow rw&#x27; osd &#x27;allow rwx pool=liverpool&#x27;</span><br><span class="hljs-comment"># ceph auth caps client.brian-manager mon &#x27;allow *&#x27; osd &#x27;allow *&#x27;</span><br><span class="hljs-comment"># ceph auth caps client.ringo mon &#x27; &#x27; osd &#x27; &#x27;</span><br></code></pre></td></tr></table></figure>

<h3 id="4、删除用户"><a href="#4、删除用户" class="headerlink" title="4、删除用户"></a>4、删除用户</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># ceph auth del &#123;TYPE&#125;.&#123;ID&#125;</span><br>其中， &#123;TYPE&#125; 是 client，osd，mon 或 mds 的其中一种。&#123;ID&#125; 是用户的名字或守护进程的 ID 。<br></code></pre></td></tr></table></figure>

<h2 id="13-增加和删除-Monitor"><a href="#13-增加和删除-Monitor" class="headerlink" title="13.增加和删除 Monitor"></a>13.增加和删除 Monitor</h2><blockquote>
<p>一个集群可以只有一个 monitor，推荐生产环境至少部署 3 个。 Ceph 使用 Paxos 算法的一个变种对各种 map 、以及其它对集群来说至关重要的信息达成共识。建议（但不是强制）部署奇数个 monitor 。Ceph 需要 mon 中的大多数在运行并能够互相通信，比如单个 mon，或 2 个中的 2 个，3 个中的 2 个，4 个中的 3 个等。初始部署时，建议部署 3 个 monitor。后续如果要增加，请一次增加 2 个.</p>
</blockquote>
<h3 id="1、新增一个-monitor"><a href="#1、新增一个-monitor" class="headerlink" title="1、新增一个 monitor"></a>1、新增一个 monitor</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># ceph-deploy mon create $hostname</span><br>注意：执行ceph-deploy之前要进入之前安装时候配置的目录。/my-cluster<br></code></pre></td></tr></table></figure>

<h3 id="2、删除-Monitor"><a href="#2、删除-Monitor" class="headerlink" title="2、删除 Monitor"></a>2、删除 Monitor</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># ceph-deploy mon destroy $hostname</span><br>注意： 确保你删除某个 Mon 后，其余 Mon 仍能达成一致。如果不可能，删除它之前可能需要先增加一个。<br></code></pre></td></tr></table></figure>

<h1 id="四、集群监控管理"><a href="#四、集群监控管理" class="headerlink" title="四、集群监控管理"></a>四、集群监控管理</h1><h2 id="1-集群整体运行状态"><a href="#1-集群整体运行状态" class="headerlink" title="1.集群整体运行状态"></a>1.集群整体运行状态</h2><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">[root@cephnode01 ~]<span class="hljs-comment"># ceph -s</span><br>cluster:<br>    <span class="hljs-built_in">id</span>:     8230a918-a0de-4784-9ab8-cd2a2b8671d0<br>    health: HEALTH_WARN<br>            application not enabled on 1 pool(s)<br><br>  services:<br>    mon: 3 daemons, quorum cephnode01,cephnode02,cephnode03 (age 27h)<br>    mgr: cephnode01(active, since 53m), standbys: cephnode03, cephnode02<br>    osd: 4 osds: 4 up (since 27h), 4 <span class="hljs-keyword">in</span> (since 19h)<br>    rgw: 1 daemon active (cephnode01)<br><br>  data:<br>    pools:   6 pools, 96 pgs<br>    objects: 235 objects, 3.6 KiB<br>    usage:   4.0 GiB used, 56 GiB / 60 GiB avail<br>    pgs:     96 active+clean<br><br>    <span class="hljs-built_in">id</span>：集群ID<br>    health：集群运行状态，这里有一个警告，说明是有问题，意思是pg数大于pgp数，通常此数值相等。<br>    mon：Monitors运行状态。<br>    osd：OSDs运行状态。<br>    mgr：Managers运行状态。<br>    mds：Metadatas运行状态。<br>    pools：存储池与PGs的数量。<br>    objects：存储对象的数量。<br>    usage：存储的理论用量。<br>    pgs：PGs的运行状态<br><br>~]$ ceph -w<br>~]$ ceph health detail<br></code></pre></td></tr></table></figure>

<h2 id="2-PG-状态"><a href="#2-PG-状态" class="headerlink" title="2.PG 状态"></a>2.PG 状态</h2><blockquote>
<p>查看 pg 状态查看通常使用下面两个命令即可，dump 可以查看更详细信息</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">~]$ ceph pg dump<br>~]$ ceph pg <span class="hljs-built_in">stat</span><br></code></pre></td></tr></table></figure>

<h2 id="3-Pool-状态"><a href="#3-Pool-状态" class="headerlink" title="3.Pool 状态"></a>3.Pool 状态</h2><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">~]$ ceph osd pool stats<br>~]$ ceph osd pool stats<br></code></pre></td></tr></table></figure>

<h2 id="4-OSD-状态"><a href="#4-OSD-状态" class="headerlink" title="4.OSD 状态"></a>4.OSD 状态</h2><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">~]$ ceph osd <span class="hljs-built_in">stat</span><br>~]$ ceph osd dump<br>~]$ ceph osd tree<br>~]$ ceph osd <span class="hljs-built_in">df</span><br></code></pre></td></tr></table></figure>

<h2 id="5-Monitor-状态和查看仲裁状态"><a href="#5-Monitor-状态和查看仲裁状态" class="headerlink" title="5.Monitor 状态和查看仲裁状态"></a>5.Monitor 状态和查看仲裁状态</h2><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">~]$ ceph mon <span class="hljs-built_in">stat</span><br>~]$ ceph mon dump<br>~]$ ceph quorum_status<br></code></pre></td></tr></table></figure>

<h2 id="6-集群空间用量"><a href="#6-集群空间用量" class="headerlink" title="6.集群空间用量"></a>6.集群空间用量</h2><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">~]$ ceph <span class="hljs-built_in">df</span><br>~]$ ceph <span class="hljs-built_in">df</span> detail<br></code></pre></td></tr></table></figure>

<h1 id="五、集群配置管理-临时和全局，服务平滑重启"><a href="#五、集群配置管理-临时和全局，服务平滑重启" class="headerlink" title="五、集群配置管理(临时和全局，服务平滑重启)"></a>五、集群配置管理(临时和全局，服务平滑重启)</h1><blockquote>
<p>有时候需要更改服务的配置，但不想重启服务，或者是临时修改。这时候就可以使用 tell 和 daemon 子命令来完成此需求。</p>
</blockquote>
<h2 id="1-查看运行配置"><a href="#1-查看运行配置" class="headerlink" title="1.查看运行配置"></a>1.查看运行配置</h2><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">命令格式：<br><span class="hljs-comment"># ceph daemon &#123;daemon-type&#125;.&#123;id&#125; config show</span><br><br>命令举例：<br><span class="hljs-comment"># ceph daemon osd.0 config show</span><br></code></pre></td></tr></table></figure>

<h2 id="2-tell-子命令格式"><a href="#2-tell-子命令格式" class="headerlink" title="2.tell 子命令格式"></a>2.tell 子命令格式</h2><blockquote>
<p>使用 tell 的方式适合对整个集群进行设置，使用 * 号进行匹配，就可以对整个集群的角色进行设置。而出现节点异常无法设置时候，只会在命令行当中进行报错，不太便于查找。</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">命令格式：<br><span class="hljs-comment"># ceph tell &#123;daemon-type&#125;.&#123;daemon id or *&#125; injectargs --&#123;name&#125;=&#123;value&#125; [--&#123;name&#125;=&#123;value&#125;]</span><br>命令举例：<br><span class="hljs-comment"># ceph tell osd.0 injectargs --debug-osd 20 --debug-ms 1</span><br></code></pre></td></tr></table></figure>

<ul>
<li>daemon-type：为要操作的对象类型如 osd、mon、mds 等。</li>
<li>daemon id：该对象的名称，osd 通常为 0、1 等，mon 为 ceph -s 显示的名称，这里可以输入*表示全部。</li>
<li>injectargs：表示参数注入，后面必须跟一个参数，也可以跟多个</li>
</ul>
<h2 id="3-daemon-子命令"><a href="#3-daemon-子命令" class="headerlink" title="3.daemon 子命令"></a>3.daemon 子命令</h2><ul>
<li>使用 daemon 进行设置的方式就是一个个的去设置，这样可以比较好的反馈，此方法是需要在设置的角色所在的主机上进行设置。</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">命令格式：<br><span class="hljs-comment"># ceph daemon &#123;daemon-type&#125;.&#123;id&#125; config set &#123;name&#125;=&#123;value&#125;</span><br>命令举例：<br><span class="hljs-comment"># ceph daemon mon.ceph-monitor-1 config set mon_allow_pool_delete false</span><br></code></pre></td></tr></table></figure>

<h2 id="4-集群操作"><a href="#4-集群操作" class="headerlink" title="4.集群操作"></a>4.集群操作</h2><ul>
<li>命令包含 start、restart、status</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment">#1.启动所有守护进程</span><br>systemctl start ceph.target<br><br><span class="hljs-comment">#2.按类型启动守护进程</span><br>systemctl start ceph-mgr.target<br>systemctl start ceph-osd@<span class="hljs-built_in">id</span><br>systemctl start ceph-mon.target<br>systemctl start ceph-mds.target<br>systemctl start ceph-radosgw.target<br></code></pre></td></tr></table></figure>

<h2 id="5-添加和删除-OSD"><a href="#5-添加和删除-OSD" class="headerlink" title="5.添加和删除 OSD"></a>5.添加和删除 OSD</h2><h3 id="1-添加-OSD"><a href="#1-添加-OSD" class="headerlink" title="1).添加 OSD"></a>1).添加 OSD</h3><ul>
<li><p>纵向扩容(会导致数据的重分布)</p>
</li>
<li><p>生产环境下最好的做法就是不要一次性添加大量的 osd，最好逐步添加等待数据同步后再进行添加操作</p>
<ul>
<li>当影响生产数据时候临时可以停止同步：ceph osd set [nobackfill|norebalance],unset 取消对应的参数</li>
</ul>
</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment">#1.格式化磁盘</span><br>ceph-volume lvm zap /dev/sd&lt;<span class="hljs-built_in">id</span>&gt;<br><br><span class="hljs-comment">#2.进入到ceph-deploy执行目录/my-cluster，添加OSD</span><br>ceph-deploy osd create --data /dev/sd&lt;<span class="hljs-built_in">id</span>&gt; <span class="hljs-variable">$hostname</span><br></code></pre></td></tr></table></figure>

<h3 id="2-删除-OSD"><a href="#2-删除-OSD" class="headerlink" title="2).删除 OSD"></a>2).删除 OSD</h3><ul>
<li><p>如果机器有盘坏了可以使用 dmdsg 查看坏盘</p>
</li>
<li><p>存在一种情况就是某 osd 的写入延迟大盘有坏道很大可能会拖垮 ceph 集群：</p>
<ul>
<li>ceph osd tree: 查看当前集群的 osd 状态</li>
<li>ceph osd perf: 查看当前的 OSD 的延迟</li>
</ul>
</li>
<li><p>当某一快 osd 踢出集群时候立即做数据重分布(默认 10 分钟)</p>
</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">1、调整osd的crush weight为 0<br>ceph osd crush reweight osd.&lt;ID&gt; 0.0<br><br>2、将osd进程stop<br>systemctl stop ceph-osd@&lt;ID&gt;<br><br>3、将osd设置out(将会出发数据重分布)<br>ceph osd out &lt;ID&gt;<br><br>4、从crushmap中踢出osd<br><span class="hljs-comment"># 查看运行视图的osd状态</span><br>ceph osd crush dump|less<br>ceph osd crush <span class="hljs-built_in">rm</span> &lt;osd&gt;.<span class="hljs-built_in">id</span><br><br>5、从tree树中删除osd<br>ceph osd <span class="hljs-built_in">rm</span> &lt;osd&gt;.<span class="hljs-built_in">id</span><br><br>6、(选用)立即执行删除OSD中数据<br>ceph osd purge osd.&lt;ID&gt; --yes-i-really-mean-it<br><br>7、卸载磁盘<br>umount /var/lib/ceph/osd/ceph-？<br><br>8.从认证中删除磁盘对应的key<br><span class="hljs-comment"># 查看认证的列表</span><br>ceph auth list<br>ceph auth <span class="hljs-built_in">rm</span> &lt;osd&gt;.<span class="hljs-built_in">id</span><br></code></pre></td></tr></table></figure>

<h2 id="6-扩容-PG"><a href="#6-扩容-PG" class="headerlink" title="6.扩容 PG"></a>6.扩容 PG</h2><ul>
<li>扩容大小取跟它接近的 2 的 N 次方</li>
<li>在更改 pool 的 PG 数量时，需同时更改 PGP 的数量。PGP 是为了管理 placement 而存在的专门的 PG，它和 PG 的数量应该保持一致。如果你增加 pool 的 pg_num，就需要同时增加 pgp_num，保持它们大小一致，这样集群才能正常 rebalancing。</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">ceph osd pool <span class="hljs-built_in">set</span> &#123;pool-name&#125; pg_num 128<br>ceph osd pool <span class="hljs-built_in">set</span> &#123;pool-name&#125; pgp_num 128<br></code></pre></td></tr></table></figure>

<h1 id="六、调优"><a href="#六、调优" class="headerlink" title="六、调优"></a>六、调优</h1><h2 id="1-系统层面调优"><a href="#1-系统层面调优" class="headerlink" title="1.系统层面调优"></a>1.系统层面调优</h2><ul>
<li><p>选择正确的 CPU 和内存。OSD、MON 和 MDS 节点具有不同的 CPU 和内存需求</p>
<ul>
<li>mon 的需求和 osd 的总个数有关需要的是计算力</li>
<li>mds 对 CPU 和内存要求很高，会将大量的元数据缓存到自己的内存中，存储元数据的尽量的使用 ssd</li>
<li>osd 最低要求 1H2G 的配置例如：24 块硬盘最少是 24H36G,磁盘方面必须高 I&#x2F;O 有多好上多好</li>
</ul>
</li>
<li><p>尽可能关闭 NUMA</p>
</li>
<li><p>规划好存储节点的数据以及各节点的磁盘要求（不考虑钱忽略）</p>
</li>
<li><p>磁盘的选择尽可能在成本、吞吐量和延迟之间找到良好的平衡</p>
</li>
<li><p>journal 日志应该使用 SSD</p>
</li>
<li><p>如果交换机支持（MTU 9000），则启用巨型帧(减少数据的分片)，前提是 ceph 在一个单独的网络环境中切有独立交换机。</p>
</li>
<li><p>启用 ntp。Ceph 对时间敏感,集群网络至少 10GB 带宽</p>
</li>
</ul>
<h3 id="1-系统调优工具"><a href="#1-系统调优工具" class="headerlink" title="1).系统调优工具"></a>1).系统调优工具</h3><ul>
<li>使用 tuned-admin 工具，它可帮助系统管理员针对不同的工作负载进行系统调优</li>
<li>tuned-admin 使用的 profile 默认存放在&#x2F;usr&#x2F;lib&#x2F;tuned&#x2F;目录中，可以参考其模板来自定义 profile</li>
<li>对于 ceph 而言，network-latency 可以改进全局系统延迟，network-throughput 可以改进全局系统吞吐量,如果两个都开启可以使用 Custom 自定义模式</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 列出现有可用的profile</span><br>tuned-adm list<br><br><span class="hljs-comment"># 查看当前生效的profile</span><br>tuned-adm active<br><br><span class="hljs-comment"># 使用指定的profile</span><br>tuned-admin profile profile-name<br><br><span class="hljs-comment"># 禁用所有的profile</span><br>tuned-admin off<br></code></pre></td></tr></table></figure>

<h3 id="2-I-x2F-O-调度算法"><a href="#2-I-x2F-O-调度算法" class="headerlink" title="2).I&#x2F;O 调度算法"></a>2).I&#x2F;O 调度算法</h3><ul>
<li>noop：电梯算法，实现了一个简单的 FIFO 队列。基于 SSD 的磁盘，推荐使用这种调度方式</li>
<li>Deadline：截止时间调度算法，尽力为请求提供有保障的延迟。对于 Ceph，基于 sata 或者 sas 的驱动器，应该首选这种调度方式</li>
<li>cfq：完全公平队列，适合有许多进程同时读取和写入大小不等的请求的磁盘，也是默认的通用调度算法</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment">#查看当前系统支持的调度算法：</span><br>    dmesg|grep -I scheduler<br><br><span class="hljs-comment">#查看指定磁盘使用的调度算法：</span><br>    <span class="hljs-built_in">cat</span> /sys/block/磁盘设备号/queue/scheduler<br><br><span class="hljs-comment">#修改调度算法</span><br>    <span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;deadline&quot;</span> &gt; /sys/block/vdb/queue/scheduler<br>    vim /etc/default/grub<br>        GRUB_CMDLINE_LINUX=<span class="hljs-string">&quot;elevator=deadline numa=off&quot;</span><br></code></pre></td></tr></table></figure>

<h3 id="3-网络-IO-子系统调优"><a href="#3-网络-IO-子系统调优" class="headerlink" title="3).网络 IO 子系统调优"></a>3).网络 IO 子系统调优</h3><ul>
<li>用于集群的网络建议尽可能使用 10Gb 网络</li>
</ul>
<p>以下参数用于缓冲区内存管理</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment">#设置OS接收缓冲区的内存大小，第一个值告知内核一个TCP socket的最小缓冲区空间，第二值为默认缓冲区空间，第三个值是最大缓冲区空间</span><br>net.ipv4.tcp_wmem<br><br><span class="hljs-comment">#设置Os发送缓冲区的内存大小</span><br>net.ipv4.tcp_rmem<br><br><span class="hljs-comment">#定义TCP stack如何反应内存使用情况</span><br>net.ipv4.tcp_mem<br></code></pre></td></tr></table></figure>

<ul>
<li>交换机启用大型帧</li>
</ul>
<blockquote>
<p>默认情况下，以太网最大传输数据包大小为 1500 字节。为提高吞吐量并减少处理开销，一种策略是将以太网网络配置为允许设备发送和接收更大的巨型帧。</p>
</blockquote>
<ul>
<li>在使用巨型帧的要谨慎，因为需要硬件支持，且全部以太网口配置为相同的巨型帧 MTU 大小。</li>
</ul>
<h3 id="4-虚拟内存调优"><a href="#4-虚拟内存调优" class="headerlink" title="4).虚拟内存调优"></a>4).虚拟内存调优</h3><p>设置较低的比率会导致高频但用时短的写操作，这适合 Ceph 等 I&#x2F;O 密集型应用。设置较高的比率会导致低频但用时长的写操作，这会产生较小的系统开销，但可能会造成应用响应时间变长</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment">#脏内存占总系统总内存的百分比，达到此比率时内核会开始在后台写出数据</span><br>vm.dirty_background_ratio<br><br><span class="hljs-comment">#脏内存占总系统总内存的百分比，达到此比率时写入进程停滞，而系统会将内存页清空到后端存储</span><br>vm.dirty_ratio<br><br><span class="hljs-comment">#控制交换分区的使用,生产中建议完全关闭，会拖慢系统运行速度</span><br>vm.swappiness<br><br><span class="hljs-comment">#系统尽力保持可用状态的RAM大小。在一个RAM大于48G的系统上，建议设置为4G</span><br>vm.min_free_kbytes<br></code></pre></td></tr></table></figure>

<h2 id="2-Ceph-本身调优"><a href="#2-Ceph-本身调优" class="headerlink" title="2.Ceph 本身调优"></a>2.Ceph 本身调优</h2><h3 id="1-最佳实践"><a href="#1-最佳实践" class="headerlink" title="1).最佳实践"></a>1).最佳实践</h3><ul>
<li>MON 的性能对集群总体性能至关重要，应用部署于专用节点，为确保正确仲裁，数量应为奇数个</li>
<li>在 OSD 节点上，操作系统、OSD 数据、OSD 日志应当位于独立的磁盘上，以确保满意的吞吐量</li>
<li>在集群安装后，需要监控集群、排除故障并维护，尽管 Ceph 具有自愈功能。如果发生性能问题，首先在磁盘、网络和硬件层面上调查。然后逐步转向 RADOS 块设备和 Ceph 对象网关</li>
</ul>
<h3 id="2-影响-I-x2F-O-的-6-大操作"><a href="#2-影响-I-x2F-O-的-6-大操作" class="headerlink" title="2).影响 I&#x2F;O 的 6 大操作"></a>2).影响 I&#x2F;O 的 6 大操作</h3><ul>
<li>业务数据写入</li>
<li>数据恢复</li>
<li>数据回填</li>
<li>数据重平衡</li>
<li>数据一致性校验</li>
<li>快照清理</li>
</ul>
<h3 id="3-OSD-生产建议"><a href="#3-OSD-生产建议" class="headerlink" title="3).OSD 生产建议"></a>3).OSD 生产建议</h3><ul>
<li>更快的日志性能可以改进响应时间，建议将单独的低延迟 SSD 或者 NVMe 设备用于 OSD 日志。</li>
<li>多个日志可以共享同一 SSD，以降低存储基础架构的成本。但是不能将过多 OSD 日志放在同一设备上。</li>
<li>建议每个 SATA OSD 设备不超过 6 个 OSD 日志，每个 NVMe 设备不超过 12 个 OSD 日志。</li>
<li>需要说明的是，当用于托管日志的 SSD 或者 NVMe 设备故障时，使用它托管其日志的所有 OSD 也都变得不可用</li>
</ul>
<h3 id="4-硬件建议"><a href="#4-硬件建议" class="headerlink" title="4).硬件建议"></a>4).硬件建议</h3><ul>
<li>将一个 raid1 磁盘用于 ceph 操作系统</li>
<li>每个 OSD 一块硬盘，尽量将 SSD 或者 NVMe 用于日志</li>
<li>使用多个 10Gb 网卡，每个网络一个双链路绑定（建议生产环境 2 个网卡 4 个光模块，2 个万兆口做为数据的交换，2 个万兆口做业务流量）</li>
<li>每个 OSD 预留 1 个 CPU,每个逻辑核心 1GHz，分配 16GB 内存，外加每个 OSD 2G 内存</li>
</ul>
<h3 id="5-RBD-生产建议"><a href="#5-RBD-生产建议" class="headerlink" title="5).RBD 生产建议"></a>5).RBD 生产建议</h3><ul>
<li>块设备上的工作负载通常是 I&#x2F;O 密集型负载，例如在 OpenStack 中虚拟机上运行数据库。</li>
<li>对于 RBD,OSD 日志应当位于 SSD 或者 NVMe 设备上</li>
<li>对后端存储，可以使用不同的存储设备以提供不同级别的服务</li>
</ul>
<h3 id="6-对象网关生产建议"><a href="#6-对象网关生产建议" class="headerlink" title="6).对象网关生产建议"></a>6).对象网关生产建议</h3><ul>
<li>Ceph 对象网关工作负载通常是吞吐密集型负载。但是其 bucket 索引池为 I&#x2F;O 密集型工作负载模式。应当将这个池存储在 SSD 设备上</li>
<li>Ceph 对象网关为每个存储桶维护一个索引。Ceph 将这一索引存储在 RADOS 对象中。当存储桶存储数量巨大的对象时（超过 100000 个），索引性能会降低，因为只有一个 RADOS 对象参与所有索引操作。</li>
<li>Ceph 可以在多个 RADOS 对象或分片中保存大型索引。可以在 ceph.conf 中设置 rgw_override_bucket_index_max_shards 配置参数来启用该功能。此参数的建议值是存储桶中预计对象数量除以 10000</li>
<li>当索引变大，Ceph 通常需要重新划分存储桶。rgw_dynamic_resharding 配置控制该功能，默认为 true</li>
</ul>
<h3 id="7-CephFS-生产建议"><a href="#7-CephFS-生产建议" class="headerlink" title="7).CephFS 生产建议"></a>7).CephFS 生产建议</h3><ul>
<li>存放目录结构和其他索引的元数据池可能会成为 CephFS 的瓶颈。因此，应该将 SSD 设备用于这个池</li>
<li>每个 MDS 维护一个内存中缓存 ，用于索引节点等不同类型的项目。Ceph 使用 mds_cache_memory_limit 配置参数限制这一缓存的大小。其默认值为 1GB，可以在需要时调整，得不得超过系统总内存数</li>
</ul>
<h3 id="8-Monitor-生产建议"><a href="#8-Monitor-生产建议" class="headerlink" title="8).Monitor 生产建议"></a>8).Monitor 生产建议</h3><ul>
<li>最好为每个 MON 一个独立的服务器&#x2F;虚拟机</li>
<li>小型和中型集群，使用 10000RPM 的磁盘，大型集群使用 SSD</li>
<li>CPU 使用方面：使用一个多核 CPU，最少 16G 内存，最好不要和 osd 存放在同一个服务器上</li>
</ul>
<h3 id="9-将-OSD-日志迁移到-SSD"><a href="#9-将-OSD-日志迁移到-SSD" class="headerlink" title="9).将 OSD 日志迁移到 SSD"></a>9).将 OSD 日志迁移到 SSD</h3><p>强烈建议生产中千万不要这么干，一定在集群初始化的时候就定制好</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment">#集群中设置标志位停止指定的osd使用</span><br>ceph osd <span class="hljs-built_in">set</span> noout<br><br><span class="hljs-comment">#停止osd的进程</span><br>systemctl stop ceph-osd@3<br><br><span class="hljs-comment">#将所有的日志做刷盘处理，刷盘到osd中</span><br>ceph-osd -i 3 --flush-journal<br><br><span class="hljs-comment">#删除该osd现有的日志</span><br><span class="hljs-built_in">rm</span> -f /var/lib/ceph/osd/ceph-3/journal<br><br><span class="hljs-comment">#/dev/sdc1为SSD盘创建一个软连接</span><br><span class="hljs-built_in">ln</span> -s /dev/sdc1 /var/lib/ceph/osd/ceph-3/journal<br><br><span class="hljs-comment">#刷出日志</span><br>ceph-osd -i 3 --mkjournal<br><br><span class="hljs-comment">#启动osd</span><br>systemctl start ceph-osd@3<br><br><span class="hljs-comment">#移除标志位</span><br>ceph osd <span class="hljs-built_in">unset</span> noout<br></code></pre></td></tr></table></figure>

<h3 id="10-存储池中-PG-的计算方法"><a href="#10-存储池中-PG-的计算方法" class="headerlink" title="10).存储池中 PG 的计算方法"></a>10).存储池中 PG 的计算方法</h3><ul>
<li><p>通常，计算一个池中应该有多少个归置组的计算方法 &#x3D; 100 * OSDs(个数) &#x2F; size(副本数)</p>
</li>
<li><p>一种比较通用的取值规则：</p>
<ul>
<li>少于 5 个 OSD 时可把 pg_num 设置为 128</li>
<li>OSD 数量在 5 到 10 个时，可把 pg_num 设置为 512</li>
<li>OSD 数量在 10 到 50 个时，可把 pg_num 设置为 4096</li>
<li>OSD 数量大于 50 时，建议自行计算</li>
</ul>
</li>
<li><p>自行计算 pg_num 聚会时的工具</p>
<ul>
<li>pgcalc：<a href="https://ceph.com/pgcalc/">https://ceph.com/pgcalc/</a></li>
<li>cephpgc：<a href="https://access.redhat.com/labs/cephpgc/">https://access.redhat.com/labs/cephpgc/</a></li>
</ul>
</li>
<li><p>注意：在实际的生产环境中我们很难去预估需要多少个 pool，每个 pool 所占用的数据大小的百分百。所以正常情况下需要在特定的情况选择动态扩缩容 pg 的大小</p>
</li>
</ul>
<h3 id="11-PG-与-PGP"><a href="#11-PG-与-PGP" class="headerlink" title="11).PG 与 PGP"></a>11).PG 与 PGP</h3><blockquote>
<p>通常而言，PG 与 PGP 是相同的当我们为一个池增加 PG 时，PG 会开始分裂，这个时候，OSD 上的数据开始移动到新的 PG，但总体而言，此时，数据还是在一个 OSD 的不同 PG 中迁移而我们一旦同时增加了 PGP，则 PG 开始在多个 OSD 上重平衡，这时会出现跨 OSD 的数据迁移</p>
</blockquote>
<ul>
<li>ceph osd pool create poolName PgNum PgpNum</li>
<li>当变动 pg 数量只是针对当前的特定池中的 osd 发生变动影响范围只是一个池的 pg 平衡</li>
<li>正常情况下一个 osd 最多承载 100 个 pg</li>
<li>当 pgp 发生大变动的时候会导致原本这个池中的 pg 变动导致池中 osd，过载或者有很大剩余性能，ceph 集群会将过大的性能均衡到各个性能使用小的 osd 上，这个时候就会发生数据的大规模迁移，大量的 i&#x2F;O 写入会占有网络带宽会严重影响使用中的 pg 性能导致阻塞发生。</li>
<li>建议的做法是将 pg_num 直接设置为希望作为最终值的 PG 数量，而 PGP 的数量应当慢慢增加，以确保集群不会因为一段时间内的大量数据重平衡而导致的性能下降</li>
</ul>
<h3 id="12-Ceph-生产网络建议"><a href="#12-Ceph-生产网络建议" class="headerlink" title="12).Ceph 生产网络建议"></a>12).Ceph 生产网络建议</h3><ul>
<li>尽可能使用 10Gb 网络带宽以上的万兆带宽(内网)</li>
<li>尽可能使用不同的 cluster 网络和 public 网络</li>
<li>做好必要的网络设备监控防止网络过载</li>
</ul>
<h3 id="13-OSD-和数据一致性校验"><a href="#13-OSD-和数据一致性校验" class="headerlink" title="13).OSD 和数据一致性校验"></a>13).OSD 和数据一致性校验</h3><blockquote>
<p>清理会影响 ceph 集群性能，但建议不要禁用此功能，因为它能提供完数据的完整性</p>
</blockquote>
<ul>
<li>清理：检查对象的存在性、校验和以及大小</li>
<li>深度清理：检查对象的存在性和大小，重新计算并验证对象的校验和。(最好不开严重影响性能)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment">#清理调优参数</span><br>osd_scrub_begin_hour =                    <span class="hljs-comment">#取值范围0-24</span><br>osd_scrub_end_hour = end_hbegin_hour our  <span class="hljs-comment">#取值范围0-24</span><br>osd_scrub_load_threshold                  <span class="hljs-comment">#当系统负载低于多少的时候可以清理，默认为0.5</span><br>osd_scrub_min_interval                    <span class="hljs-comment">#多久清理一次，默认是一天一次（前提是系统负载低于上一个参数的设定）</span><br>osd_scrub_interval_randomize_ratio        <span class="hljs-comment">#在清理的时候，随机延迟的值，默认是0.5</span><br>osd_scrub_max_interval                    <span class="hljs-comment">#清理的最大间隔时间，默认是一周（如果一周内没清理过，这次就必须清理，不管负载是多少）</span><br>osd_scrub_priority                        <span class="hljs-comment">#清理的优先级，默认是5</span><br>osd_deep_scrub_interal                    <span class="hljs-comment">#深度清理的时间间隔，默认是一周</span><br>osd_scrub_sleep                           <span class="hljs-comment">#当有磁盘读取时，则暂停清理，增加此值可减缓清理的速度以降低对客户端的影响，默认为0,范围0-1</span><br></code></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment">#显示最近发生的清理和深度清理</span><br>ceph pg dump all  <span class="hljs-comment"># 查看LAST_SCRUB和LAST_DEEP_SCRUB</span><br><span class="hljs-comment">#-将清理调度到特定的pg</span><br>ceph pg scrub pg-id<br><span class="hljs-comment">#将深度清理调度到特定的pg</span><br>ceph pg deep-scrub pg-id<br><span class="hljs-comment">#为设定的池设定清理参数</span><br>ceph osd pool <span class="hljs-built_in">set</span> &lt;pool-name&gt; &lt;parameter&gt; &lt;value&gt;<br>    noscrub <span class="hljs-comment"># 不清理，默认为false</span><br>    nodeep-scrub <span class="hljs-comment"># 不深度清理，默认为false</span><br>    scrub_min_interval <span class="hljs-comment"># 如果设置为0，则应用全局配置osd_scrub_min_interval</span><br>    scrub_max_interval <span class="hljs-comment"># 如果设置为0，则应用全局配置osd_scrub_max_interval</span><br>    deep_scrub_interval <span class="hljs-comment"># 如果设置为0，则应用全局配置osd_scrub_interval</span><br></code></pre></td></tr></table></figure>

<h3 id="14-快照的生产建议"><a href="#14-快照的生产建议" class="headerlink" title="14).快照的生产建议"></a>14).快照的生产建议</h3><ul>
<li>快照在池级别和 RBD 级别上提供。当快照被移除时，ceph 会以异步操作的形式删除快照数据，称为快照修剪进程</li>
<li>为减轻快照修剪进程会影响集群总体性能。可以通过配置<code>osd_snap_trim_sleep</code>来在有客户端读写操作的时候暂停修剪，参数的值范围是<code>0</code>到<code>1</code></li>
<li>快照修剪的优先级通过使用<code>osd_snap_trim_priority</code>参数控制，默认为<code>5</code></li>
</ul>
<h3 id="15-保护数据和-osd"><a href="#15-保护数据和-osd" class="headerlink" title="15).保护数据和 osd"></a>15).保护数据和 osd</h3><ul>
<li><p>需要控制回填和恢复操作，以限制这些操作的影响</p>
</li>
<li><p>回填发生于新的 osd 加入集群时，或者 osd 死机并且 ceph 将其 pg 分配到其他 osd 时。在这种场景中，ceph 必须要在可用的 osd 之间复制对象副本</p>
</li>
<li><p>恢复发生于新的 osd 已有数据时，如出现短暂停机。在这种情形下，ceph 会简单的重放 pg 日志</p>
<ul>
<li>管理回填和恢复操作的配置项</li>
</ul>
</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment">#用于限制每个osd上用于回填的并发操作数，默认为1</span><br>osd_max_backfills<br><br><span class="hljs-comment">#用于限制每个osd上用于恢复的并发操作数，默认为3</span><br>osd_recovery_max_active<br><br><span class="hljs-comment">#恢复操作的优先级，默认为3</span><br>osd_recovery_op_priority<br></code></pre></td></tr></table></figure>

<h3 id="16-OSD-数据存储后端"><a href="#16-OSD-数据存储后端" class="headerlink" title="16).OSD 数据存储后端"></a>16).OSD 数据存储后端</h3><blockquote>
<p>BlueStore 管理一个，两个或（在某些情况下）三个存储设备。在最简单的情况下，BlueStore 使用单个（主）存储设备。存储设备通常作为一个整体使用，BlueStore 直接占用完整设备。该主设备通常由数据目录中的块符号链接标识。数据目录挂载成一个 tmpfs，它将填充（在启动时或 ceph-volume 激活它时）所有常用的 OSD 文件，其中包含有关 OSD 的信息，例如：其标识符，它所属的集群，以及它的私钥。还可以使用两个额外的设备部署 BlueStore</p>
</blockquote>
<ul>
<li>WAL 设备（在数据目录中标识为 block.wal）可用于 BlueStore 的内部日志或预写日志。只有设备比主设备快（例如，当它在 SSD 上并且主设备是 HDD 时），使用 WAL 设备是有用的。</li>
<li>数据库设备（在数据目录中标识为 block.db）可用于存储 BlueStore 的内部元数据。 BlueStore（或者更确切地说，嵌入式 RocksDB）将在数据库设备上放置尽可能多的元数据以提高性能。如果数据库设备填满，元数据将写到主设备。同样，数据库设备要比主设备更快，则提供数据库设备是有帮助的。</li>
<li>如果只有少量快速存储可用（例如，小于 1GB），我们建议将其用作 WAL 设备。如果还有更多，配置数据库设备会更有意义。 BlueStore 日志将始终放在可用的最快设备上，因此使用数据库设备将提供与 WAL 设备相同的优势，同时还允许在其中存储其他元数据。</li>
<li>正常 L 版本推荐使用 filestore，M 版本可以考虑使用 bluestore</li>
<li>推荐优化文章：<a href="https://www.cnblogs.com/luxiaodai/p/10006036.html#_lab2_1_9">https://www.cnblogs.com/luxiaodai/p/10006036.html#_lab2_1_9</a></li>
</ul>
<h3 id="17-关于性能测试"><a href="#17-关于性能测试" class="headerlink" title="17).关于性能测试"></a>17).关于性能测试</h3><ul>
<li>推荐使用 fio 参考阿里云文档：<a href="https://help.aliyun.com/document_detail/95501.html?spm=a2c4g.11174283.6.659.38b44da2KZr2Sn">https://help.aliyun.com/document_detail&#x2F;95501.html?spm&#x3D;a2c4g.11174283.6.659.38b44da2KZr2Sn</a></li>
<li>dd</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">echo</span> 3 &gt; /proc/sys/vm/drop_caches<br><span class="hljs-built_in">dd</span> <span class="hljs-keyword">if</span>=/dev/zero of=/var/lib/ceph/osd/ceph-0/test.img bs=4M count=1024 oflag=direct<br><span class="hljs-built_in">dd</span> <span class="hljs-keyword">if</span>=/var/lib/ceph/osd/ceph-0/test.img of=/dev/null bs=4M count=1024 oflag=direct<br></code></pre></td></tr></table></figure>

<ul>
<li>rados bench 性能测试</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">rados bench -p &lt;pool_name&gt; &lt;seconds&gt; &lt;write|<span class="hljs-built_in">seq</span>|rand&gt; -b &lt;block size&gt; -t --no-cleanup<br>    pool_name 测试所针对的池<br>    seconds 测试所持续的时间，以秒为单位<br>    &lt;write|<span class="hljs-built_in">seq</span>|rand&gt; 操作模式，分别是写、顺序读、随机读<br>    -b &lt;block_size&gt; 块大小，默认是4M<br>    -t 读/写的并行数，默认为16<br>    --no-cleanup 表示测试完成后不删除测试用的数据。在做读测试之前，需要使用该参数来运行一遍写测试来产生测试数据，在全部测试完成以后，可以行rados -p &lt;pool_name&gt; cleanup来清理所有测试数据<br><br><span class="hljs-comment">#示例：</span><br>rados bench -p rbd 10 write --no-cleanup<br>rados bench -p rbd 10 <span class="hljs-built_in">seq</span><br></code></pre></td></tr></table></figure>

<ul>
<li>rbd bench 性能测试</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">rbd bench -p &lt;pool_name&gt; &lt;image_name&gt; --io-type &lt;write|<span class="hljs-built_in">read</span>&gt; --io-size &lt;size&gt; --io-threads &lt;num&gt; --io-total &lt;size&gt; --io-pattern &lt;<span class="hljs-built_in">seq</span>|rand&gt;<br>    --io-type 测试类型，读/写<br>    --io-size 字节数，默认4096<br>    --io-threads 线程数，默认16<br>    --io-total  读/写的总大小，默认1GB<br>    --io-pattern  读/写的方式，顺序还是随机<br><br><span class="hljs-comment">#示例：</span><br>https://edenmal.moe/post/2017/Ceph-rbd-bench-Commands/<br></code></pre></td></tr></table></figure>

<h2 id="3-设置集群的标志"><a href="#3-设置集群的标志" class="headerlink" title="3.设置集群的标志"></a>3.设置集群的标志</h2><p><strong>flag 操作</strong></p>
<ul>
<li><p>只能对整个集群操作，不能针对单个 osd</p>
<ul>
<li>ceph osd set</li>
<li>ceph osd unset</li>
</ul>
</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment">#示例：</span><br>ceph osd <span class="hljs-built_in">set</span> nodown<br>ceph osd <span class="hljs-built_in">unset</span> nodown<br>ceph -s<br></code></pre></td></tr></table></figure>

<table>
<thead>
<tr>
<th align="left">标志名称</th>
<th align="left">含义用法详解</th>
</tr>
</thead>
<tbody><tr>
<td align="left">noup</td>
<td align="left">OSD 启动时，会将自己在 MON 上标识为 UP 状态，设置该标志位，则 OSD 不会被自动标识为 up 状态</td>
</tr>
<tr>
<td align="left">nodown</td>
<td align="left">OSD 停止时，MON 会将 OSD 标识为 down 状态，设置该标志位，则 MON 不会将停止的 OSD 标识为 down 状态，设置 noup 和 nodown 可以防止网络抖动</td>
</tr>
<tr>
<td align="left">noout</td>
<td align="left">设置该标志位，则 mon 不会从 crush 映射中删除任何 OSD。对 OSD 作维护时，可设置该标志位，以防止 CRUSH 在 OSD 停止时自动重平衡数据。OSD 重新启动时，需要清除该 flag</td>
</tr>
<tr>
<td align="left">noin</td>
<td align="left">设置该标志位，可以防止数据被自动分配到 OSD 上</td>
</tr>
<tr>
<td align="left">norecover</td>
<td align="left">设置该 flag，禁止任何集群恢复操作。在执行维护和停机时，可设置该 flag</td>
</tr>
<tr>
<td align="left">nobackfill</td>
<td align="left">禁止数据回填</td>
</tr>
<tr>
<td align="left">noscrub</td>
<td align="left">禁止清理操作。清理 PG 会在短期内影响 OSD 的操作。在低带宽集群中，清理期间如果 OSD 的速度过慢，则会被标记为 down。可以该标记来防止这种情况发生</td>
</tr>
<tr>
<td align="left">nodeep-scrub</td>
<td align="left">禁止深度清理</td>
</tr>
<tr>
<td align="left">norebalance</td>
<td align="left">禁止重平衡数据。在执行集群维护或者停机时，可以使用该 flag</td>
</tr>
<tr>
<td align="left">pause</td>
<td align="left">设置该标志位，则集群停止读写，但不影响 osd 自检</td>
</tr>
<tr>
<td align="left">full</td>
<td align="left">标记集群已满，将拒绝任何数据写入，但可读</td>
</tr>
</tbody></table>
<h1 id="参考文档"><a href="#参考文档" class="headerlink" title="参考文档"></a>参考文档</h1><p><a href="https://poph163.com/category/%e5%88%86%e5%b8%83%e5%bc%8f%e5%ad%98%e5%82%a8/">https://poph163.com/category/分布式存储/</a></p>
]]></content>
      <categories>
        <category>Ceph 学习</category>
      </categories>
      <tags>
        <tag>ceph</tag>
      </tags>
  </entry>
  <entry>
    <title>vi 常用命令</title>
    <url>/2021/07/31/linux/vi%20%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4/</url>
    <content><![CDATA[<h2 id="Vi-常用命令"><a href="#Vi-常用命令" class="headerlink" title="Vi 常用命令"></a>Vi 常用命令</h2><h3 id="进入-vi-的命令"><a href="#进入-vi-的命令" class="headerlink" title="进入 vi 的命令"></a>进入 vi 的命令</h3><table>
<thead>
<tr>
<th align="center">命令</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td align="center">vi filename</td>
<td>打开或新建文件，并将光标置于第一行首</td>
</tr>
<tr>
<td align="center">vi +n filename</td>
<td>打开文件，并将光标置于第 n 行首</td>
</tr>
<tr>
<td align="center">vi + filename</td>
<td>打开文件，并将光标置于最后一行首</td>
</tr>
<tr>
<td align="center">vi +&#x2F;pattern filename</td>
<td>打开文件，并将光标置于第一个与 pattern 匹配的串处</td>
</tr>
<tr>
<td align="center">vi -r filename</td>
<td>在上次正用 vi 编辑时发生系统崩溃，恢复 filename</td>
</tr>
<tr>
<td align="center">vi filename….filename</td>
<td>打开多个文件，依次进行编辑</td>
</tr>
</tbody></table>
<span id="more"></span>

<h3 id="移动光标类命令"><a href="#移动光标类命令" class="headerlink" title="移动光标类命令"></a>移动光标类命令</h3><table>
<thead>
<tr>
<th align="center">命令</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td align="center">h</td>
<td>光标左移一个字符</td>
</tr>
<tr>
<td align="center">l</td>
<td>光标右移一个字符</td>
</tr>
<tr>
<td align="center">space</td>
<td>光标右移一个字符</td>
</tr>
<tr>
<td align="center">Backspace</td>
<td>光标左移一个字符</td>
</tr>
<tr>
<td align="center">k 或 Ctrl+p</td>
<td>光标上移一行</td>
</tr>
<tr>
<td align="center">j 或 Ctrl+n</td>
<td>光标下移一行</td>
</tr>
<tr>
<td align="center">Enter</td>
<td>光标下移一行</td>
</tr>
<tr>
<td align="center">w 或 W</td>
<td>光标右移一个字至字首</td>
</tr>
<tr>
<td align="center">b 或 B</td>
<td>光标左移一个字至字首</td>
</tr>
<tr>
<td align="center">e 或 E</td>
<td>光标右移一个字至字尾</td>
</tr>
<tr>
<td align="center">(</td>
<td>光标移至句首</td>
</tr>
<tr>
<td align="center">)</td>
<td>光标移至句尾</td>
</tr>
<tr>
<td align="center">{</td>
<td>光标移至段落开头</td>
</tr>
<tr>
<td align="center">}</td>
<td>光标移至段落结尾</td>
</tr>
<tr>
<td align="center">nG</td>
<td>光标移至第 n 行首</td>
</tr>
<tr>
<td align="center">G</td>
<td>到最后一行</td>
</tr>
<tr>
<td align="center">n+</td>
<td>光标下移 n 行</td>
</tr>
<tr>
<td align="center">n-</td>
<td>光标上移 n 行</td>
</tr>
<tr>
<td align="center">n$</td>
<td>光标移至第 n 行尾</td>
</tr>
<tr>
<td align="center">H</td>
<td>光标移至屏幕顶行</td>
</tr>
<tr>
<td align="center">M</td>
<td>光标移至屏幕中间行</td>
</tr>
<tr>
<td align="center">L</td>
<td>光标移至屏幕最后行</td>
</tr>
<tr>
<td align="center">0</td>
<td>（注意是数字零）光标移至当前行首</td>
</tr>
<tr>
<td align="center">$</td>
<td>光标移至当前行尾</td>
</tr>
</tbody></table>
<h3 id="屏幕翻滚类命令"><a href="#屏幕翻滚类命令" class="headerlink" title="屏幕翻滚类命令"></a>屏幕翻滚类命令</h3><table>
<thead>
<tr>
<th align="center">命令</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td align="center">Ctrl+u</td>
<td>向文件首翻半屏</td>
</tr>
<tr>
<td align="center">Ctrl ＋ b</td>
<td>向文件首翻一屏</td>
</tr>
<tr>
<td align="center">Ctrl+d</td>
<td>向文件尾翻半屏</td>
</tr>
<tr>
<td align="center">Ctrl+f</td>
<td>向文件尾翻一屏</td>
</tr>
<tr>
<td align="center">nz</td>
<td>将第 n 行滚至屏幕顶部，不指定 n 时将当前行滚至屏幕顶部</td>
</tr>
</tbody></table>
<h3 id="插入文本类命令"><a href="#插入文本类命令" class="headerlink" title="插入文本类命令"></a>插入文本类命令</h3><table>
<thead>
<tr>
<th align="center">命令</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td align="center">i</td>
<td>在光标前</td>
</tr>
<tr>
<td align="center">I</td>
<td>在当前行首</td>
</tr>
<tr>
<td align="center">a</td>
<td>光标后</td>
</tr>
<tr>
<td align="center">A</td>
<td>在当前行尾</td>
</tr>
<tr>
<td align="center">o</td>
<td>在当前行之下新开一行</td>
</tr>
<tr>
<td align="center">O</td>
<td>在当前行之上新开一行</td>
</tr>
<tr>
<td align="center">r</td>
<td>替换当前字符</td>
</tr>
<tr>
<td align="center">R</td>
<td>替换当前字符及其后的字符，直至按 ESC 键</td>
</tr>
<tr>
<td align="center">s</td>
<td>从当前光标位置处开始，以输入的文本替代指定数目的字符</td>
</tr>
<tr>
<td align="center">S</td>
<td>删除指定数目的行，并以所输入文本代替之</td>
</tr>
<tr>
<td align="center">ncw 或 nCW</td>
<td>修改指定数目的字</td>
</tr>
<tr>
<td align="center">nCC</td>
<td>修改指定数目的行</td>
</tr>
</tbody></table>
<h3 id="删除命令"><a href="#删除命令" class="headerlink" title="删除命令"></a>删除命令</h3><table>
<thead>
<tr>
<th align="center">命令</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td align="center">ndw 或 ndW</td>
<td>删除光标处开始及其后的 n-1 个字</td>
</tr>
<tr>
<td align="center">do</td>
<td>删至行首</td>
</tr>
<tr>
<td align="center">d$</td>
<td>删至行尾</td>
</tr>
<tr>
<td align="center">ndd</td>
<td>删除当前行及其后 n-1 行</td>
</tr>
<tr>
<td align="center">x 或 X</td>
<td>删除一个字符，x 删除光标后的，而 X 删除光标前的</td>
</tr>
<tr>
<td align="center">Ctrl+u</td>
<td>删除输入方式下所输入的文本</td>
</tr>
</tbody></table>
<h3 id="搜索及替换命令"><a href="#搜索及替换命令" class="headerlink" title="搜索及替换命令"></a>搜索及替换命令</h3><table>
<thead>
<tr>
<th align="center">命令</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td align="center">&#x2F;pattern</td>
<td>从光标开始处向文件尾搜索 pattern</td>
</tr>
<tr>
<td align="center">?pattern</td>
<td>从光标开始处向文件首搜索 pattern</td>
</tr>
<tr>
<td align="center">n</td>
<td>在同一方向重复上一次搜索命令</td>
</tr>
<tr>
<td align="center">N</td>
<td>在反方向上重复上一次搜索命令</td>
</tr>
<tr>
<td align="center">：s&#x2F;p1&#x2F;p2&#x2F;g</td>
<td>将当前行中所有 p1 均用 p2 替代</td>
</tr>
<tr>
<td align="center">：n1,n2s&#x2F;p1&#x2F;p2&#x2F;g</td>
<td>将第 n1 至 n2 行中所有 p1 均用 p2 替代</td>
</tr>
<tr>
<td align="center">：g&#x2F;p1&#x2F;s&#x2F;&#x2F;p2&#x2F;g</td>
<td>将文件中所有 p1 均用 p2 替换</td>
</tr>
</tbody></table>
<h3 id="选项设置"><a href="#选项设置" class="headerlink" title="选项设置"></a>选项设置</h3><table>
<thead>
<tr>
<th align="center">命令</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td align="center">all</td>
<td>列出所有选项设置情况</td>
</tr>
<tr>
<td align="center">term</td>
<td>设置终端类型</td>
</tr>
<tr>
<td align="center">ignorance</td>
<td>在搜索中忽略大小写</td>
</tr>
<tr>
<td align="center">list</td>
<td>显示制表位(Ctrl+I)和行尾标志（$)</td>
</tr>
<tr>
<td align="center">number</td>
<td>显示行号</td>
</tr>
<tr>
<td align="center">report</td>
<td>显示由面向行的命令修改过的数目</td>
</tr>
<tr>
<td align="center">terse</td>
<td>显示简短的警告信息</td>
</tr>
<tr>
<td align="center">warn</td>
<td>在转到别的文件时若没保存当前文件则显示 NO write 信息</td>
</tr>
<tr>
<td align="center">nomagic</td>
<td>允许在搜索模式中，使用前面不带“\”的特殊字符</td>
</tr>
<tr>
<td align="center">nowrapscan</td>
<td>禁止 vi 在搜索到达文件两端时，又从另一端开始</td>
</tr>
<tr>
<td align="center">mesg</td>
<td>允许 vi 显示其他用户用 write 写到自己终端上的信息</td>
</tr>
</tbody></table>
<h3 id="最后行方式命令"><a href="#最后行方式命令" class="headerlink" title="最后行方式命令"></a>最后行方式命令</h3><table>
<thead>
<tr>
<th align="center">命令</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td align="center">：n1,n2 co n3</td>
<td>将 n1 行到 n2 行之间的内容拷贝到第 n3 行下</td>
</tr>
<tr>
<td align="center">：n1,n2 m n3</td>
<td>将 n1 行到 n2 行之间的内容移至到第 n3 行下</td>
</tr>
<tr>
<td align="center">：n1,n2 d</td>
<td>将 n1 行到 n2 行之间的内容删除</td>
</tr>
<tr>
<td align="center">：w</td>
<td>保存当前文件</td>
</tr>
<tr>
<td align="center">：e filename</td>
<td>打开文件 filename 进行编辑</td>
</tr>
<tr>
<td align="center">：x</td>
<td>保存当前文件并退出</td>
</tr>
<tr>
<td align="center">：q</td>
<td>退出 vi</td>
</tr>
<tr>
<td align="center">：q!</td>
<td>不保存文件并退出 vi</td>
</tr>
<tr>
<td align="center">：!command</td>
<td>执行 shell 命令 command</td>
</tr>
<tr>
<td align="center">：n1,n2 w!command</td>
<td>将文件中 n1 行至 n2 行的内容作为 command 的输入并执行之，若不指定 n1，n2，则表示将整个文件内容作为 command 的输入</td>
</tr>
<tr>
<td align="center">：r!command</td>
<td>将命令 command 的输出结果放到当前行</td>
</tr>
</tbody></table>
<h3 id="寄存器操作"><a href="#寄存器操作" class="headerlink" title="寄存器操作"></a>寄存器操作</h3><table>
<thead>
<tr>
<th align="center">命令</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td align="center">“?nyy</td>
<td>将当前行及其下 n 行的内容保存到寄存器？中，其中?为一个字母，n 为一个数字</td>
</tr>
<tr>
<td align="center">“?nyw</td>
<td>将当前行及其下 n 个字保存到寄存器？中，其中?为一个字母，n 为一个数字</td>
</tr>
<tr>
<td align="center">“?nyl</td>
<td>将当前行及其下 n 个字符保存到寄存器？中，其中?为一个字母，n 为一个数字</td>
</tr>
<tr>
<td align="center">“?p</td>
<td>取出寄存器？中的内容并将其放到光标位置处。这里？可以是一个字母，也可以是一个数字</td>
</tr>
<tr>
<td align="center">ndd</td>
<td>将当前行及其下共 n 行文本删除，并将所删内容放到 1 号删除寄存器中</td>
</tr>
</tbody></table>
<h3 id="保存退出"><a href="#保存退出" class="headerlink" title="保存退出"></a>保存退出</h3><table>
<thead>
<tr>
<th align="center">命令</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td align="center">zz</td>
<td>保存并退出</td>
</tr>
<tr>
<td align="center">:w filename</td>
<td>写入文件</td>
</tr>
<tr>
<td align="center">:W</td>
<td>写入文件</td>
</tr>
<tr>
<td align="center">:x</td>
<td>保存(如果当前文件修改过)并退出</td>
</tr>
<tr>
<td align="center">:q!</td>
<td>不保存文件，直接退出</td>
</tr>
<tr>
<td align="center">:q</td>
<td>退出 vi</td>
</tr>
</tbody></table>
]]></content>
      <categories>
        <category>linux</category>
      </categories>
      <tags>
        <tag>vi</tag>
      </tags>
  </entry>
</search>
